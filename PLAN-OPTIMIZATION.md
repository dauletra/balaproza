# План оптимизации `balaproza_v1`

> Рабочий документ. Живёт между сессиями. Статус каждого шага отмечается прямо
> здесь (`[ ]` / `[~]` / `[x]`), с датой и коммитом.
> Аудит проведён 2026-08-27 на `main` @ `bc8072f`, суита зелёная (1156 тестов, 22.7 с).

---

## 0. Исходные цифры (baseline)

| Метрика | Значение |
|---|---|
| Python, без тестов | 9 276 строк (≈5 354 кода, ≈2 516 прозы = **32 %**) |
| `core/models.py` | 1 777 строк (870 кода, 580 прозы) |
| Тесты | **1 156** в 25 файлах, 7 655 строк |
| Шаблоны | 139 файлов, 10 318 строк |
| Документация | 21 файл, 3 613 строк + `CLAUDE.md` 493 |
| Демо-корпус | `_corpus.py` 1 361 строка литералов |
| Миграций | 15 |
| Запросов: главная / глава / профиль | 22 / 31 / 14 |

Целевые цифры — в §7.

---

## 1. Что в проекте сделано хорошо (не трогать)

Это не вежливость, а границы работ: перечисленное ниже переживает рефакторинг
без изменений.

- **`core/domain/` не знает о хранилище.** Правила выражены константами и
  чистыми функциями, миграция с заглушек на PostgreSQL их не тронула. Это
  единственный слой, который доказал свою стоимость на практике.
- **`core/links.py`** — `CatalogState` (frozen dataclass) + один список осей
  `CATALOG_AXES` на проект. URL-состояние каталога решено правильно.
- **`core/queries/catalog.py`** — выдача честно живёт в QuerySet: аннотации
  (`effective_chars`, `read_minutes_db`, `in_open_contest`), объём подзапросом,
  а не `Sum` по join. Пагинация на уровне БД.
- **Пакет `core/views/`** — модуль на раздел, имена собраны в `__init__`.
- **`Story.apply_moderation`** — статус и уведомление одним движением.
- **Индексы (0008) и `pg_trgm` (0009)** поставлены под реальные запросы страниц.
- **Тест бюджета запросов** (`test_query_budget.py`) — самый ценный файл в
  суите; он один нашёл 59 запросов на профиле.

---

## 2. Диагноз: чем болеет кодовая база

### 2.1 Главная проблема — функциональная, а не архитектурная

**Половина write-путей продукта не существует. UI для них есть.**

| Кнопка / поведение в UI | Что происходит на самом деле |
|---|---|
| «Сақтау» (в библиотеку) | `LibraryEntry` пишется **только сидом**. Кнопка — Alpine-состояние в браузере. |
| «Жазылу» (подписка на автора) | `Follow` пишется **только сидом**. |
| Счётчик оқылым | `Story.views` / `recent_views` **не инкрементируются нигде**. |
| Прогресс чтения «X / N» | `ReadingProgress` пишется **только сидом** — не двигается. |
| «Барлығын оқылды деп белгілеу» | `Notification.read` не выставляется **нигде** вне корпуса. |
| `Story.chapters` (число частей на карточке) | `save_chapter` его **не обновляет**. Автор пишет 5 глав → карточка говорит «0 бөлім». |

Следствия каскадом:
- дефолтная сортировка каталога («Қазір танымал» по `recent_views`) в проде
  будет вечно показывать порядок из сида;
- витрины тегов (`usage_count` / `weekly_count`) тоже не пересчитываются
  (известный долг DEC-31);
- **демо-корпус на 1 361 строку — это протез вместо функциональности.** Он
  такой большой именно потому, что заменяет ненаписанные write-пути. Отсюда же
  50 тестов `test_corpus.py`, сторожащих целостность литералов.

Это первое, что надо чинить: любая оптимизация вокруг остаётся полировкой
неработающего.

### 2.2 Слой данных: два несовместимых контракта под одним фасадом

`core/data.py` экспортирует 133 имени. Часть возвращает `QuerySet`
(`catalog.py`), часть — `list` (`author.py`, `profile.py`, `contests.py`,
`story.py`, `tags.py` — 17 мест `return list(...)`).

У списка нет кэша и нет ленивости. Вызывающая сторона этого не видит, зовёт
хелпер столько раз, сколько удобно читать, — и профиль стоил **59 запросов**.
Лечение выбрали компенсирующее: класс `AuthorFacts`, снимок на один запрос,
который надо не забыть передать параметром `facts=` в **девять** хелперов.

Это лечение симптома. `AuthorFacts` — костыль поверх решения «фасад отдаёт
list». QuerySet кэширует себя сам, лениво, без параметра.

### 2.3 Скрытый контракт «аннотация или fallback»

Семь свойств моделей написаны так:

```python
annotated = getattr(self, 'effective_chars', None)
if annotated is not None:
    return annotated
return sum(c.char_count for c in self.chapter_set.all())   # N+1
```

(`works_count`, `has_any_chapter`, `in_open_contest`, `effective_chars`,
`submission_count`, `_my_reaction`, `_my_vote`)

Проблема: контракт между `queries/` и `models.py` **невидим и не проверяем**.
Забыл аннотацию — страница работает, но делает N запросов. Или, хуже,
`_my_reaction` молча отвечает «голоса нет», хотя голос есть. Ловится только
`assertNumQueries` — то есть постфактум и только на тех страницах, что внесены
в бюджет.

### 2.4 Толстая модель: 1 777 строк, из них 870 кода

`Contest` — 26 свойств. Часть из них делает запросы при каждом обращении:

```python
@property
def awards(self):        return list(self.award_set.all())
@property
def awards_by_slug(self): return {a.slug: a for a in self.awards}   # ещё раз
@property
def other_editions(self): return list(Contest.objects.filter(...))  # запрос на обращение
```

`Story` — 19 свойств, `Chapter` — 6, `ChapterPoll` — 6. Значительная часть —
**презентация**: `format_label`, `format_badge_label`, `updated_label`,
`opens_on_label`, `closes_on_label`, `results_on_label`, `timing_line`,
`eligibility_line`, `char_count_formatted`, `reading_meta_label`. Это работа
шаблонного фильтра или `domain/formatting`, а не модели.

### 2.5 View-слой: ноль Django-форм, ноль декораторов

- `login_required` — **не используется ни разу**. Приватные страницы
  (`/me/`, `/write/*`, `/library/`, `/notifications/`) отдают гостю 200 с пустым
  контекстом. Отсюда `if username else []` — **15 раз** в одном
  `profile_me`.
- `require_POST` — используется один раз (`logout`). Остальные восемь
  POST-эндпоинтов проверяют `request.method != 'POST'` вручную и на GET молча
  редиректят.
- Django-формы — **не используются нигде**. `request.POST.get(...)` встречается
  32 раза, валидация написана руками списками `errors`, `RASTER_ONLY`
  вызывается вручную, потому что валидаторы поля не срабатывают вне ModelForm
  (это прямо признано в комментарии `views/write.py:131`).
  `story_settings` — 60 строк ручного разбора формы.

`ModelForm` убирает здесь порядка 150 строк и заодно чинит валидацию файлов.

### 2.6 Непоследовательный 404

`profile_other` → `Http404`. `story_detail`, `genre_detail`, `tag_detail`,
`collection_detail`, `contest_detail` → **200 с «табылмады»** внутри страницы.
Из-за этого каждый шаблон обязан выдерживать `story=None`, а поисковик
индексирует любой выдуманный slug.

### 2.7 Найденные баги

| # | Где | Что |
|---|---|---|
| B1 | `queries/catalog.py:282` | `related_stories` фильтрует `status='Published'` — литерал, запрещённый DEC-37. **Все сериалы выпадают из блока «Что дальше»**. Должно быть `status__in=PUBLIC_STATUSES`. |
| B2 | `queries/write.py:save_chapter` | не обновляет `Story.chapters` → «0 бөлім» на карточке. |
| B3 | `views/profile.py:profile_people` | `fetch(username)` + `f(username)` в цикле `people_items` — список подписчиков берётся 3 раза за рендер. |
| B4 | `templates/.../story_detail.html:84,85` | `stringformat:"s"` вместо `spaced`/`compact_count` — расходится с соседней строкой 83. |
| B5 | — | `RASTER_ONLY` на `FileField` не срабатывает при прямом `save()`; защита держится на ручном вызове в двух views. Третий забудут. |
| **B6** | `tests/runner.py:67` | **`seed_demo` пишет в БОЕВУЮ базу разработчика.** `setup_databases` зовёт сид безусловно, а Django создаёт тестовую базу только если тесты её просят (`get_databases(suite)`). Модули из чистого `unittest.TestCase` (`test_filters`, `test_docs_sync`, часть `test_template_lint`) базу не просят — тестовой базы нет, и сид уходит в `DATABASE_URL`. Сейчас это видно только потому, что dev-база отстала на 4 миграции и сид падает с `core_user.age не существует`; на актуальной базе он молча перезаписал бы данные разработки. Заодно сломана документированная команда `manage.py test core.tests.<file>`. **Чинить первым.** |

| **B9** | `core/tests/test_catalog.py:CatalogPresets` | `test_counts_are_real` сверял счётчик чипа (`p['count']` — про всю выдачу) с `len(results)` — то есть с одной страницей. Работало, пока ни один пресет не перебирал `PAGE_SIZE=20`; первый же перебравший показал ошибку. **Исправлено в шаге 1.8** — сверка идёт с `total_results`. |
| **B7** | `queries/story.py:collection_by_slug` | Страница одной подборки — **76 запросов**. `Collection.objects.filter(slug=…).first()` шла без `prefetch_related`, а `Collection.stories` тянул автора, жанр, теги и объём на каждую работу. Соседний `all_collections()` prefetch делает; одиночный резолв — нет. Не в бюджете запросов, поэтому и не был замечен. **Исправлено в шаге 2.5**: `Prefetch('item_set__story', queryset=all_stories())`, 76 → 5. |
| **B10** | `queries/catalog.py:filter_catalog` | Витрина тега — **27 запросов** против 18 у жанра. «Принят ли тег» проверялось отдельным `EXISTS`, а страница зовёт `filter_catalog` семь раз: выдача плюс счётчик каждого из шести пресетов. **Исправлено в шаге 2.5**: оба условия на одной связке (`tags__slug=…, tags__status='accepted'`), 27 → 20. Найдено заведением бюджета, а не чтением кода. |

> Побочно: dev-база отстаёт на миграции 0012–0015 (`showmigrations core`).
> `manage.py migrate` до начала работ.

### 2.8 Проза вместо кода

32 % непустых строк Python — комментарии и docstring'и. В `models.py` — 580
строк прозы на 870 строк кода. Комментарии написаны как эссе («Раньше он был
размазан по views.py пятью приватными функциями…») — это **история изменений в
исходнике**, место которой в git log и в реестре решений.

Это не стилистика. Это стоимость: каждая правка кода требует правки абзаца
рядом, иначе комментарий начинает врать. Часть уже врёт (`views/story.py:32`
ссылается на `SAMPLE_PROGRESS` — литерал, живущий теперь только в сиде).

### 2.9 Тесты: 1 156 штук ради ~300 сценариев

Стиль — «класс на состояние страницы, метод на одно утверждение, запрос в
`setUp`»:

```python
class SearchResultsWithMatches(TestCase):
    def setUp(self):  self.response = self.client.get(...)   # ← 4 раза
    def test_200(self): ...
    def test_query_echo(self): ...
    def test_shows_matching_story(self): ...
    def test_no_empty_state(self): ...
```

Django создаёт экземпляр на каждый метод → **четыре HTTP-запроса ради четырёх
утверждений об одном ответе**. Плюс:

- `test_200` дублирует `test_urls_smoke.py` в десятках мест;
- тесты завязаны на литералы корпуса («Алыс жағалауларда», «Рысқали»,
  `aidana`) — правка демо-данных ломает суиту;
- `test_docs_sync.py` делает документацию **исполняемой зависимостью кода**:
  счётчик «1156 тестов в 25 файлах» в `README.md` и `CLAUDE.md` обязан
  совпадать, иначе красный тест. Изменение числа тестов ⇒ правка двух
  markdown-файлов;
- `test_corpus.py` (50) сторожит целостность демо-данных — нужен только
  потому, что корпус подменяет функциональность (§2.1);
- `test_data_facade.py` (4) проверяет, что импорты в `data.py` существуют. Это
  делает интерпретатор.

Скорость не проблема (22,7 с). Проблема — **1 156 точек, которые надо чинить
при каждом изменении разметки**.

### 2.10 Документация: 3 613 строк, 21 файл, встроенное правило самосинхронизации

`CLAUDE.md` — 493 строки, включающие полное дерево проекта с комментариями
к каждому файлу, реестр из ~20 DEC, список 25 тест-файлов, стилистические
правила шаблонов. Это дублирует то, что видно из кода, и обязано обновляться
в том же коммите (правило самой `CLAUDE.md`).

`docs/` содержит два **завершённых** плана миграции (19, 20 — 326 строк),
карту реализации `14` (366 строк «требование → view → шаблон → тест»,
устаревающую при каждом переименовании) и контракт тестирования `15`
(260 строк — описание того, что и так видно в `core/tests/`).

---

## 3. Целевая архитектура

```
core/
├── domain/        ← без изменений: правила, константы, чистые функции
├── models.py      ← только поля, Meta, __str__, инварианты и мутации.
│                    Презентация → templatetags/domain.formatting.
│                    Списочные свойства → менеджеры/аннотации.
├── managers.py    ← NEW: StoryQuerySet (public/with_reading_effort/for_catalog),
│                    ContestQuerySet, заменяет половину queries/*.py
├── queries/       ← остаётся только там, где запрос не выражается менеджером;
│                    ВСЁ возвращает QuerySet, не list. AuthorFacts исчезает.
├── forms.py       ← NEW: StoryForm, ChapterForm, ProfileForm, SubmissionForm,
│                    CommentForm. Убирает ~150 строк ручного разбора.
├── links.py       ← без изменений
├── views/         ← @login_required / @require_POST, формы, get_object_or_404
└── tests/         ← ~10 файлов, ~250 сценарных тестов
```

Ключевые смены контракта:

1. **Фасад `data.py` отдаёт QuerySet.** `AuthorFacts` удаляется, параметр
   `facts=` уходит из девяти сигнатур.
2. **Аннотации перестают быть скрытыми.** Либо запрос всегда приходит из
   менеджера, который их ставит (`Story.objects.for_catalog()`), либо свойство
   падает громко, а не уходит в N+1.
3. **Приватная страница отвечает 302, публичная несуществующая — 404.**
4. **Модель не форматирует строки.**

---

## 4. Пошаговый план

Порядок обязателен: шаги 1–2 сокращают то, что дальше пришлось бы
переписывать дважды. Каждый шаг — отдельный коммит, суита зелёная после
каждого.

### Шаг 1. Баги и дыры write-пути ⚠️ делать первым
- [x] **1.0** B6: сид в раннере — только если тестовая база создана
      (`if not config: return config` перед `call_command('seed_demo')`).
      Dev-база догнана `manage.py migrate` (0012–0015).
      Проверено: `manage.py test core.tests.test_filters` проходит за 0,8 с
      и не трогает `DATABASE_URL` (23 работы / 6 пользователей до и после).
- [x] **1.1** B1: `related_stories` больше не сужает выдачу до литерала
      `'Published'` — публичность приходит из `catalog_base()`. Заодно две
      выборки («тот же жанр» + «добор популярным») схлопнуты в одну с
      ключом сортировки `other_genre`: порядок прежний, а второй
      `prefetch_related('tags')` ушёл. Бюджеты: страница произведения
      19 → **18**, глава 31 → **30**. Регрессия закрыта
      `test_story.RelatedStoriesCoverAllPublicStatuses` — по всему корпусу,
      а не по одному слагу.
- [x] **1.2** B2: **поле `Story.chapters` удалено** (миграция 0016, DEC-51).
      Число частей выводится: аннотация `chapter_count`
      (`queries/catalog.chapter_count_subquery`) в выдаче, `chapter_set.count()`
      у одиночного объекта. Вместе с колонкой ушла оценка объёма
      `chapters * 1800` — оценивать ненаписанное нечем.
      Аннотация проставлена там, где строка **про** работу и `select_related`
      аннотировать связанный объект не умеет (`library_of`,
      `reading_progress_of`, `book_of_week` — счёт едет своей строкой и
      садится на `story.chapter_count`, без второго запроса).
      Бюджеты не выросли: главная 22, каталог 18, глава 30.
      Закрыто `test_write.ChapterEditorSavesADraft.test_written_chapters_show_up_in_the_count`
      (пишем три главы — видим «3 бөлім») и `test_corpus.ChapterCountIsDerived`.
      Последний заменил `DeclaredChapterCountMatchesLoadedChapters` — сторожа
      расхождения, которому больше нечего сторожить.
- [x] **1.3** B3: `profile_people` — открытый список берётся один раз, во
      втором сегменте стоит настоящий счётчик (`followers_count_of` /
      `following_count_of`), а не `len()` от заново вытянутой выборки.
      Страница 5 → **4** запроса, и второй запрос из `COUNT` вместо
      списка пользователей с аннотацией работ у каждого. Заведён бюджет
      `test_query_budget.test_profile_people` — его у страницы не было.
- [x] **1.4** B4: `stringformat` в позиции метрики убран — шапка
      произведения показывала «12482 оқылым» строкой ниже «12,5 мың
      реакция». Плюс четыре метрики кабинета для единообразия. Осталось
      законное применение — `{% with chap=N|stringformat:"d" %}` для
      сборки `?chapter=N`; правило в `CLAUDE.md` про это исключение не
      знало и запрещало `"d"` целиком — дописано. Закрыто
      `test_template_lint.NumbersUseTheProjectFilters`.
      **B5 не здесь** — переезд `RASTER_ONLY` в `ModelForm` остаётся за
      шагом 5.1, отдельно от форм он не делается.
- [x] **1.5** Счётчик оқылым: `record_story_view` двигает `views` и
      `recent_views` одним UPDATE (гонки складываются в базе, `updated_at`
      не трогается — чтение не есть правка). Дедупликация — сессия
      (`_count_view`, хвост из 200 работ), свой заход автору не
      засчитывается. Бюджет страницы 18 → **23**: один запрос сам
      счётчик, четыре — заведение сессии гостю, которой до этого не было
      вовсе. Дальше гость платит один запрос за загрузку сессии, как
      любой вошедший. Закрыто `test_story.ReadingCountsAsAView` (7 тестов).
      ⚠️ **Убыли у `recent_views` нет** — см. §6, вопрос 4.
- [x] **1.6** `ReadingProgress` пишется при открытии главы
      (`record_reading_progress`): глава, дата и `minutes_left` — сумма
      глав после текущей. Позиции внутри главы нет, и прикидывать её
      долю значило бы выдумывать число; `quote` по той же причине не
      сочиняется. Запись идёт **после** резолва главы: иначе первое
      знакомство с работой выглядело бы возвращением, и тизер первой
      главы не показывался бы ни разу.
      Попутно **удалена `LibraryEntry.progress_chapter`** (миграция 0017,
      DEC-52) — вторая колонка о том же факте, уже разошедшаяся в
      корпусе (`kronchessii` на полке с 2-й главой без записи о
      прогрессе). Полка получает номер аннотацией. Сид тоже перестроен:
      закладку заводит полка «оқу үстінде», отдельного `SAMPLE_PROGRESS`
      больше нет, а `minutes_left` в корпусе считается, а не пишется
      литералом (было 18, на самом деле 6).
      Заведён бюджет `test_story_page_signed_in` = **34**; закладка
      ставится сначала UPDATE'ом (вставка только на ноль строк) — на
      возвращении это один запрос вместо четырёх.
      Закрыто `test_story.ReadingRemembersWhereYouStopped` (7 тестов).
- [x] **1.7** `LibraryEntry` пишется. Кнопка «Сақтау» — настоящая форма
      (`library_toggle`), а не Alpine-состояние: она отвечает на вопрос
      присутствия и снимает работу с любой полки. Плюс **автопереходы
      BR-61 / FR-LIB-02**, которых не было вовсе: чтение кладёт работу на
      «оқу үстінде» (создавая запись, а не только двигая), открытие
      последней главы — на «оқылған», повторное чтение возвращает на
      «оқу үстінде». Правила были записаны в ТЗ и просто не исполнялись —
      выдумывать ничего не пришлось.
      Попутно в ТЗ поправлено имя полки: BR-60 звал её `finished`, в коде
      она всегда была `done`.
      Бюджет вошедшего на странице произведения 34 → **38** (первое
      открытие заводит и закладку, и полку; возвращение дешевле на шесть).
      Закрыто `test_story.StoryDetailSaveButton` (переписан с проверок
      Alpine-внутренностей на поведение) и `ReadingMovesTheWorkBetweenShelves`.
- [x] **1.8** `Follow` пишется: `follow_toggle`, обе кнопки «Жазылу»
      (шапка профиля и карточка автора) — настоящий POST с `?next=`
      вместо `action="#"` и тоста «(демо)».
      **B8 закрыт вторым вариантом**: `User.followers` остаётся колонкой,
      но пересчитывается **по строкам** — и в `toggle_follow`, и в сиде.
      Не производным свойством, как `Story.chapters`, потому что это не
      тот случай: колонку читают `ORDER BY` ленты «Жаңа авторлар» и
      `WHERE` оси каталога, а параллель здесь — `Story.likes`, а не
      `Story.chapters`. Пересчёт (а не сдвиг на единицу) делает её
      самоисправляющейся — приём `toggle_comment_like`.
      Демо от этого беднее: 8 420 оқырман у `rudazov` стали тремя. Число
      было декорацией и не соответствовало ни одной записи.
      Закрыто `test_profile.FollowingAnAuthorIsWrittenDown` (9 тестов),
      включая инвариант «колонка == число строк» на каждом шаге.
- [x] **1.9** `Notification.read` выставляется. **Не при открытии ленты**:
      BR-71 говорит «открытие уведомления», и лента, гасящая строку
      раньше, чем её прочли, обесценивает сам бейдж. Клик по предмету
      идёт через `notification_open` — снимает метку и уводит куда надо;
      адрес собирает `links.notification_href` (одна таблица соответствий
      на вью и на карточку). Кнопка «Барлығын оқылды деп белгілеу» —
      настоящий POST, режется тем же недельным окном, что и лента.
      Чужую ленту не трогает никто: `user` стоит в фильтре обоих хелперов.
      Попутно закрыт пробел BR-72a: у `comment` заголовок работы вообще не
      был ссылкой — уведомление о комментарии никуда не вело.
      Тесты `NotificationsLeadSomewhere` переписаны: они искали адрес
      предмета строкой в разметке, теперь **проходят по ссылке** и
      проверяют, куда она приводит.
      Закрыто `test_notifications.ReadingANotificationClearsIt` (8 тестов).
- [x] **1.10** `Tag.usage_count` / `weekly_count` **удалены как колонки**
      (миграция 0018, DEC-53) — считаются по `StoryTag`, аннотациями
      `usage` / `weekly`, и только по публичным работам, как у жанров.
      Пересчитывать было нечего: `weekly_count` не убывает сам по себе,
      и хранимая версия требовала бы крона, а таблица связок с датой уже
      есть. Третий случай того же хода после DEC-51 и DEC-52.
      Сид датирует связки последней правкой работы — иначе `auto_now_add`
      проставил бы всем момент запуска и обе витрины совпали бы.
      Закрыто `test_corpus.TagCountsAreDerived` (5 тестов), включая
      инвариант «недельный ≤ накопленного», который до этого был
      недостижим: числа стояли рядом независимо.

**Результат:** портал перестаёт зависеть от сида в поведении. `_corpus.py`
можно резать (шаг 8).

### Шаг 2. Сокращение тестов до минимума ✔ закрыт 2026-08-27
Целевой профиль был **≤ 320 тестов в 13 файлах** (раскладка — §2.6);
фактически вышло **556 в 12** — разбор расхождения в журнале §8.

> Цифра «≤ 250» стояла здесь до разбора и была взята на глаз. Замер дал
> 333 класса — примерно столько же сценариев, — и меньше двух-трёх тестов
> на класс не выходит без потери покрытия. Занижать цель, чтобы потом её
> не выполнить, хуже, чем поправить её один раз.

- [x] **2.1** Удалить целиком:
      `test_docs_sync.py` (284) — снимает правило самосинхронизации счётчиков;
      `test_data_facade.py` (121) — проверяет работу импорта;
      `test_corpus.py` (517) — после шага 1 корпус перестаёт быть
      функциональностью; инварианты, что останутся ценными, переезжают в
      `test_seed.py` (не более 5 тестов);
      `test_auth_links.py` (214) и `test_states.py` (189) — растворяются в
      смоуке.
- [x] **2.2** Схлопнуть «класс на состояние + метод на утверждение» в один
      сценарный тест на состояние. Механика: запрос делается один раз в теле
      теста, утверждения идут подряд, `subTest` — только там, где цикл по
      данным. Ожидаемое сокращение: 144→~35 (catalog), 137→~35 (write),
      124→~30 (story), 124→~25 (profile+library+notifications),
      110+105→~35 (contests).
- [x] **2.3** Слить файлы:
      `test_home` + `test_urls_smoke` + `test_auth` + `test_states` +
      `test_context` + `test_toast_bridge` → **`test_smoke.py`**;
      `test_profile` + `test_library` + `test_notifications` → **`test_profile.py`**;
      `test_contests` + `test_contest_submit` → **`test_contests.py`**;
      `test_models` + `test_filters` → **`test_models.py`**;
      `test_template_lint` + `test_desktop_layout` → **`test_lint.py`**.
- [x] **2.4** Убрать зависимость от литералов корпуса: тесты, которым нужны
      конкретные данные, создают их фабрикой в `tests/factories.py`, а не
      ищут «Алыс жағалауларда» в сиде. Смоук и бюджет запросов остаются на
      сиде — им нужен объём.
- [x] **2.5** `test_query_budget.py` **расширить**, а не сократить: он
      единственный ловит регрессии, которых не видно глазами. Добавить
      бюджеты на страницы, появившиеся в шаге 1.

### 2.6 Раскладка по файлам (замерено 2026-08-27, 1 207 тестов)

Что показал разбор `ast`-ом:

| Показатель | Значение |
|---|---|
| Тестов / классов | 1 207 / **333** |
| Тестов ровно с **одним** утверждением | **644** (53 %) |
| Классов, где `setUp` делает HTTP-запрос | **61** |
| Тестов под такими классами | **288** — столько же запросов вместо 61 |
| Тестов, проверяющих только `status_code` | 44 |
| Строк с литералами корпуса (`dalney-berega`, `aidana`, …) | **570** |

**Единица сокращения — класс, а не тест.** 333 класса это примерно 333
сценария, но многие соседние — один сценарий, разложенный по состояниям
(`SearchResultsEmptyQuery` / `WithMatches` / `NoMatches` — три класса на
один вопрос «что показывает поиск»).

| Файл | Тестов | Классов | Что делаем | Станет |
|---|---:|---:|---|---:|
| `test_docs_sync.py` | 8 | 7 | **удалить.** Делает документацию исполняемой зависимостью кода: счётчик «N тестов» в двух `.md` обязан совпадать. В одной сессии эта проверка потребовала семи правок счётчика | 0 |
| `test_data_facade.py` | 4 | 2 | **удалить.** Проверяет, что имена в `core/data.py` импортируются. Это делает интерпретатор | 0 |
| `test_corpus.py` | 55 | 16 | **разобрать.** Целостность демо-данных нужна была, пока корпус подменял функциональность (шаг 1 это снял). Проверки **вывода** (`ChapterCountIsDerived`, `TagCountsAreDerived`, `RecentViewsAreConsistent`) переезжают в `test_models`, остальное — 5 инвариантов в `test_seed` | 0 |
| `test_toast_bridge.py` | 5 | 1 | **свернуть** в один тест внутри `test_smoke` | 0 |
| `test_states.py` | 22 | 8 | **свернуть.** `?state=loading\|error` — демо-леса DEC-17; двадцать два теста на query-параметр | 0 |
| `test_auth_links.py` | 26 | 8 | **разнести** по разделам и смоуку: «ведёт ли страница туда, куда обещает» — вопрос раздела | 0 |
| `test_urls_smoke.py` | 5 | 4 | ядро нового `test_smoke.py` | → smoke |
| `test_auth.py` | 13 | 3 | → `test_smoke.py` | → smoke |
| `test_context.py` | 5 | 2 | → `test_smoke.py` | → smoke |
| **`test_smoke.py`** (новый) | — | — | сумма пяти файлов выше | **~20** |
| `test_home.py` | 66 | 12 | схлопнуть: 8 классов из 12 делают запрос в `setUp` — 55 тестов на 8 запросов | ~18 |
| `test_catalog.py` | 144 | 38 | схлопнуть; 76 тестов с одним утверждением | ~32 |
| `test_story.py` | 149 | 35 | схлопнуть; 78 с одним утверждением | ~32 |
| `test_write.py` | 138 | 39 | схлопнуть; 15 классов с запросом в `setUp` | ~30 |
| `test_contests.py` + `test_contest_submit.py` | 215 | 65 | слить и схлопнуть; 131 тест с одним утверждением | ~36 |
| `test_profile.py` + `test_library.py` + `test_notifications.py` | 217 | 47 | слить и схлопнуть — три раздела вошедшего | ~34 |
| `test_models.py` + `test_filters.py` + вывод из `test_corpus` | 37 | 10 | слить; это юнит-тесты, схлопывать почти нечего | ~26 |
| `test_admin.py` | 21 | 6 | схлопнуть 4 класса с `setUp`-запросом | ~12 |
| `test_seed.py` | 35 | 14 | схлопнуть + принять 5 инвариантов из `test_corpus` | ~12 |
| `test_template_lint.py` + `test_desktop_layout.py` | 33 | 18 | слить в `test_lint.py` — оба про каркас, а не про требования | ~20 |
| `test_query_budget.py` | 20 | 2 | **расширить**, а не сокращать | ~24 |
| **Итого** | **1 207** | **333** | | **~296** |

Итоговый состав `core/tests/`:
```
base.py  factories.py  runner.py
test_smoke.py        test_catalog.py     test_story.py
test_write.py        test_contests.py    test_profile.py
test_models.py       test_admin.py       test_query_budget.py
test_seed.py         test_lint.py
```

### Шаг 3. Сокращение документации до минимума ✔ закрыт 2026-08-27
Целевой профиль был **`CLAUDE.md` ≤ 120 строк, `docs/` ≤ 5 файлов, ≤ 700 строк.**
Фактически: `CLAUDE.md` **108**, `docs/` **5 файлов, 790 строк** — разбор
расхождения в журнале §8.

- [x] **3.1** Удалены девятнадцать модулей. Кроме перечисленных ниже слияний —
      `19` и `20` (планы выполнены, история в git log), `14` (карта
      «требование → код», устаревающая при каждом переименовании), `15`
      (описывал состав тестов, которого после шага 2 нет), `18` (реестр
      открытых вопросов: половина закрыта кодом, остальное живёт в §6 этого
      плана), `00` (глоссарий и матрица прав — семь строк в `architecture.md`),
      `09` (NFR сжаты до пяти строк в `spec.md`; номера сохранены — на них
      ссылаются README и код).
- [x] **3.2** `docs/ui.md` (290) — из `02`, `03`, `04`, `07`, `11`, `13`, `16`.
      Осталось только невыводимое из кода: значения токенов, оба закрытых
      реестра (роли OKLCH и металлы наград), размеры каркаса, состав нижнего
      меню, правила компонентов, тон и словари статусов/фаз/исходов.
      Перечня 62 компонентов с их параметрами здесь нет: параметры
      документированы в самих файлах блоком `{% comment %}`, и копия расходится
      с оригиналом молча — ровно так модуль `04` три месяца описывал
      компоненты, которых в коде нет.
- [x] **3.3** `docs/spec.md` (258) — `05` + `08` + NFR из `09`. Строка на
      требование, без пересказа реализации и без истории «как было раньше».
      Ни один номер FR/BR/NFR не потерян, включая аннулированные.
- [x] **3.4** `docs/10-resolved-decisions.md` — без изменений.
- [x] **3.5** `17-deployment.md` → раздел «Деплой» в `README.md` (208 строк
      вместе с установкой и тестами). Попутно из README убрана «стаб-
      авторизация»: сессия настоящая с Ф14, а раздел всё ещё описывал запись
      ключей в сессию руками.
- [x] **3.6** `CLAUDE.md` переписан: фокус, стек, команды, структура, тринадцать
      инвариантов, правило документации. Убраны дерево проекта с комментарием
      к каждому файлу, реестр DEC, список тест-файлов и все счётчики.
- [x] **3.7** Ссылки в коде переведены на новые файлы: 113 упоминаний
      `docs/NN` в комментариях и шаблонах. Номера секций (`§4.2`, `§7.6`)
      сняты вместе с модулями — они указывали на нумерацию, которой больше
      нет.

Итоговый `docs/`: `README.md` (29, индекс), `architecture.md` (127),
`spec.md` (258), `ui.md` (290), `10-resolved-decisions.md` (86).

### Шаг 4. Слой данных: QuerySet вместо list ✔ закрыт 2026-08-27
- [x] **4.1** `core/managers.py`: `StoryQuerySet` — `.public()`, `.for_card()`,
      `.with_reading_effort()`, `.by_author()`, `.sorted_by()`,
      `.latest_edited()` плюс шесть осей каталога (`with_audience`,
      `with_length`, `of_kind`, `with_badge`, `by_author_tier`, `matching`,
      `in_genre`, `with_tag`). `Story.objects` — этот queryset.
      Ушли приватные `_reading_effort` и `_sorted`; `queries/catalog.py`
      327 → 152 строки.
- [x] **4.2** `ContestQuerySet`: `.with_counts()`, `.for_card()`, `.full()`,
      `.accepting()`, `.unfinished()`, `.finished()`. Ушли `_list_base`,
      `_base`, `_counted`, `_accepting_q`.
- [x] **4.3** Четырнадцать хелперов отдают QuerySet вместо списка.
      Первым же прогоном нашлась плата за материализацию: каталог клал в
      контекст `popular_tags`, которого не читает ни один его шаблон, —
      запрос на каждой странице раздела. Бюджеты: каталог, жанр и поиск
      18 → **17**, витрина тега 20 → **19**.
- [x] **4.4** `AuthorFacts` и параметр `facts=` удалены. Снимок переехал на
      сам объект (`User.authored`, `public_works`, `own_submissions`,
      `library_entries`, `shelf()`, `reads` — `cached_property`): он и есть
      то, что страница передаёт из вызова в вызов, и забыть его нельзя.
- [x] **4.5** Слой данных принимает `User`, а не `username: str`; гость —
      `None`. Бюджеты: свой профиль 14 → **13**, чужой 9 → **8** — ушёл
      второй `SELECT` того же человека. `_current_username` остался только
      там, где нужна строка (сравнение с ником из адреса).

### Шаг 5. Формы и декораторы ✔ закрыт 2026-08-28
- [x] **5.1** `core/forms.py`: `NewStoryForm`, `StorySettingsForm`,
      `ChapterForm`, `ProfileForm`, `SubmissionForm`, `CommentForm`.
      Views короче на 55 строк, `request.POST.get` — 6 вызовов вместо 32.
      Правило записи не изменилось: **форма проверяет, `core.data` пишет**,
      `form.save()` не зовётся нигде.
      Починилось по существу: валидаторы файлов заработали сами (`RASTER_ONLY`
      держался на ручном вызове в двух местах из трёх), а BR-16 начал
      действовать — лимит аннотации в 500 знаков жил в счётчике шаблона и в
      тексте правила, но не в проверке.
      Ловушка на будущее: `ModelForm(instance=story)` раскладывает
      `cleaned_data` по экземпляру **до** `is_valid()`, а тот же экземпляр
      уходит в `update_story_settings` — «не меняем» из `clean_status`
      доехало бы до базы пустым статусом. Работа приходит аргументом `story=`.
- [x] **5.2** `@login_required` — на **действия**, а не на страницы.
      План предлагал 302 везде, но у личных разделов есть спроектированный
      ответ гостю: `components/auth_gate.html` в девяти шаблонах. Редирект
      стёр бы работающую вещь ради единообразия. `if username else []`
      исчезли раньше — вместе с `AuthorFacts` на шаге 4.
- [x] **5.3** `@require_POST` на десяти мутирующих эндпоинтах. GET на
      удаление отвечает 405, а не тихим редиректом; гость уходит на вход с
      `?next=`. Оба обещания названы тестами — держались они на том, что
      данные случайно не менялись.
- [x] **5.4** 404 вместо «200 с карточкой табылмады» у произведения, жанра,
      тега, жинақа и конкурса — как у профиля с первого дня (FR-PROF-02).
      Из шаблонов ушли ветки `if not story` во весь файл. У тега это
      правильный ответ и по существу: pending публично не существует
      (BR-TAG-07), а объяснение «проверкада» стоит на самом чипе.
      Кабинет оставлен как был: там карточка «Шығарма табылмады» отвечает
      вошедшему автору и скрывает разницу между чужим и несуществующим
      слагом (IDOR).

### Шаг 6. Разгрузка моделей ✔ закрыт 2026-08-28
- [x] **6.1** Пятнадцать презентационных свойств сняты с моделей. Подпись
      собирает фильтр (`templatetags/balaproza`), правило живёт в домене.
      Пять относительных времён схлопнулись в **два** фильтра — `ago`
      (событие) и `since` (своя работа, закладка): до этого каждая из пяти
      моделей звала `kk_ago` по-своему, и уведомление, в отличие от
      комментария, теряло минуты — свежее событие называлось «бүгін».
      Возрастная вилка и «что дальше и когда» переехали в
      `domain/contests`: их спрашивает не только шаблон, но и чек-лист
      подачи, и проверка формы.
      `Chapter.char_count_formatted` не заменён, а **удалён**: он был
      третьей формой записи числа при двух разрешённых (CLAUDE.md), и
      показывался автору — то есть по правилу проекта это `spaced`.
      `Story.format_label` удалён как мёртвый.
- [x] **6.2** Одиннадцать списочных свойств `Contest` — `cached_property`.
      `other_editions` шёл **запросом на каждое обращение**, а шаблон
      спрашивает его дважды (`{% if %}` и `{% for %}`).
      Попутно закрыт N+1, невидимый ни одному тесту: победитель на странице
      конкурса показан карточкой, а `grant_set__story` отдавал работу без
      аннотаций. Списку конкурсов эта выборка не даётся — его карточка
      называет победителя строкой заголовков. Самой дорогой странице
      раздела заведён бюджет: **17 → 15**, и цена соседнего выпуска больше
      не зависит от их числа.
- [x] **6.3** Восемь `getattr(self, '<annotation>', None)` заменены одной
      дверью `managers.from_annotation`; в `DEBUG` промах пишется в лог.
      Строка не объявляет ошибку — у одиночного объекта запрос законен;
      ошибку показывает **повтор**. Первый же проход по страницам с
      `DEBUG` нашёл два места: список участников конкурса брал работу
      через `select_related` (аннотировать связанный объект он не умеет), а
      число работ автора спрашивалось дважды на странице произведения —
      `User.works` стал `cached_property`, бюджеты 23 → 22, 35 → 34,
      38 → 37.
      Метки читателя (`_my_reaction`, `_my_vote`) отделены в
      `viewer_choice`: там промах не удорожает страницу, а **врёт** —
      пустая строка значит «не голосовал». Метка теперь ставится всегда,
      гостю тоже.
- [x] **6.4** `models.py` **1 785 → 1 588**, доля прозы 43,7 % → 34,1 %.
      Цель «≤ 900» снята как арифметически недостижимая: в файле 849 строк
      кода и ~300 пустых, то есть 1 150 — это пол при **нулевой** прозе.
      Разбор — в журнале §8. Проверено сравнением AST со снятыми
      docstring'ами: правка не тронула ни одной строки кода.

### Шаг 7. Комментарии ✔ закрыт 2026-08-28
- [x] **7.1** История изменений убрана из сорока файлов. «Раньше здесь лежал
      стаб», «до этого кнопка отвечала тостом (демо)», «прежнее BR-20
      объявляло», «в стабе выбор решался порядком объявления» — рассказ о
      пути, а не правило. Ушли и следы уже несуществующего: `stub_data.py`,
      `test_data_facade.py`, `AuthorFacts`, «Ф14/Ф15 Этап N» как датировка.
      Само правило в каждом случае осталось на месте.
- [x] **7.2** Оставлено то, что объясняет неочевидное **сейчас**: почему
      подзапрос, а не `Sum` по join (join размножит строки фильтром по
      тегам); почему `nulls_last`; почему ось возраста накопительная;
      почему сначала UPDATE, потом INSERT; почему `user` стоит в фильтре
      (IDOR, а не удобство). Проверено сравнением AST со снятыми
      docstring'ами: **ни одна строка кода не тронута** в сорока файлах.
- [x] **7.3** Проза **2 872 → 1 998 строк, 31,2 % → 24,0 %**. Цель «≤ 15 %»
      снята как арифметически недостижимая — разбор в журнале §8.

### Шаг 8. Демо-корпус и сид ✔ закрыт 2026-08-29
Целью было «≤ 500 строк, ≈15 работ, 2 конкурса, 3 подборки». Фактически
`_corpus.py` **1 361 → 1 155**, состав не сокращён — разбор в журнале §8.

- [x] **8.1** Корпусу дана история, которой у него не было.
      Четырём сериалам (`zhuldyz-kartasy`, `kokjal-anyzy`,
      `keiipkerge-hat`, `arqadagy-jaz`) дописано по три главы в
      `core/story_texts/` — до этого витрина сериалов показывала
      «0 бөлім» (§6.1, решение пользователя).
      У каждой работы теперь **своя пара дат**, создана и правлена:
      восемнадцать из двадцати трёх датировались моментом запуска сида,
      и «Жаңалары» давала случайный порядок, а «Осы аптада» почти
      совпадала с «Танымал тегтер». Три оси каталога дают три разных
      порядка, и это проверяется тестом.
      Попутно из файла ушли 77 пустых строк (остаток шага 7) и шесть
      ссылок на тест-файлы, удалённые шагом 2, — комментарии врали.
      **Состав не сокращён**, и это измеренное решение, а не пропуск:
      21 публичная работа покрывает все 12 жанров (у восьми жанров ровно
      две работы), 5 конкурсов дают все четыре фазы плюс серию из двух
      выпусков (BR-47). «15 работ, 2 конкурса» опустошили бы витрину
      жанров и убрали бы фазы, под которые в коде есть ветки.
- [x] **8.3** `StoryView` (`story`, `viewer`, `created_at`; миграция
      0019) + команда `recount_views` — §6.4, вариант (а).
      Журнал держит **только окно**: пересчёт считает по нему колонку и
      тут же чистит вышедшее, поэтому таблица растёт с трафиком двух
      недель, а не за всё время — возражение против журнала снято его же
      вычисткой. Числа окна в корпусе стали количеством строк и упали в
      десять раз: прежние тысячи были декорацией (окно видно только в
      кабинете автора), а сид платил бы за них 31 тысячей строк и
      четырнадцатью секундами на каждом прогоне суиты.
      Бюджеты 22 → 23, 34 → 35, 33 → 34.
- [x] **8.2** Закрыт ещё шагом 2.4: фабрики заведены, сценарные тесты
      создают свои данные, на сиде остались смоук и бюджет — им нужен
      объём. Оставшиеся 311 упоминаний слагов корпуса живут в
      `test_contests` и `test_profile`, где фикстура — это целый граф
      «конкурс + заявка + решение жюри», и фабрика его не удешевляет.
      Проверено делом: правки корпуса в этом шаге (даты, масштаб окна,
      новые главы) уронили пять тестов, и все пять — по существу, ни
      один не по совпадению литерала.

### Шаг 9. Фронтенд ✔ закрыт 2026-08-29
- [x] **9.1** Инлайновые `<style>`/`<script>` страницы произведения →
      `static_src/input.css` и `static/js/reader.js`. Подключение — новый
      блок `page_scripts` в `<head>`, **до** `alpine.min.js`: оба файла с
      `defer`, а `defer` исполняет в порядке документа, и регистрация
      `Alpine.data` обязана успеть до старта Alpine. Скрипт в конце
      `<body>` опоздал бы, и `storyReader` остался бы неизвестным именем —
      без ошибки в консоли. Тесты читальной поверхности сверяются с самими
      файлами, а не с разметкой ответа.
- [x] **9.2** `static/js/components.js`. Переехало то, у чего есть логика
      или что повторялось: `tagInput` (96 строк, приезжали в каждый ответ
      с редактором), `searchPopup` (51), три модалки под одним `modal(
      событие, ключи)` — у каждой стоял свой длинный `@open-….window` с
      разбором `$event.detail`, — плюс `charCounter` и `checkedBox`,
      написанные по два раза каждый.
      **План предлагал именовать все 22 `x-data`; сделано 7 из 17.**
      Однобуквенное состояние (`{ open: false }` у поповера, `{ replying:
      false }` у формы ответа) осталось в разметке: имя ему не убирает ни
      одной строки, а читать элемент пришлось бы уже в двух файлах.
      Escape стал частью самого `modal`: `withdraw_confirm` вешал его
      вручную, и забыть его значило запереть того, кто не пользуется мышью.
      Заведён линт `NamedComponentsAreRegistered` — имя в `x-data`, которого
      нет в `static/js/`, не даёт ошибки: поддерево просто не поднимается,
      а элемент выглядит рабочим. Проверяется и обратное — компонент,
      которого никто не зовёт.
- [x] **9.3** `story_detail.html` **685 → 115**: `header`, `chapter`,
      `reader_settings`, `comments`, `reading_panel` в `partials/story/`.
      Разрезано по границам поведения, а не по объёму: у каждого куска
      свой ответ на «что это делает». Из вынесенного заодно ушла история
      («раньше три оси стояли развёрнутым рядом») — то же правило, что и
      в шаге 7, только для шаблонов.

### Шаг 10. Прочее ✔ закрыт 2026-08-29
- [x] **10.1** Девятнадцать миграций → **две** (DEC-56). Не
      `squashmigrations`: пробовал, и схлопнутый файл на 714 строк
      продолжал заводить `Story.chapters`, `Tag.usage_count` и
      `LibraryEntry.progress_chapter` с индексами, чтобы следующей
      операцией их удалить — две миграции с `RunPython` стоят барьером
      для оптимизатора. Схема пересобрана начальной миграцией
      (`0001_initial`, 640 строк) плюс `0002_reference_data` — тот же
      справочник жанров без единой правки.
      База разработчика ничего не доделывает: имена совпадают со старыми
      записями, `migrate --plan` пуст. Суита прогнана на базе,
      построенной с нуля этими двумя миграциями.
- [x] **10.2** **Решено оставить в `core`** (выбор пользователя
      2026-08-29). Все три страницы живые (200, 85-218 КБ), и шаг 3.2
      убрал перечень 62 компонентов из `docs/ui.md` ровно потому, что
      копия расходится с оригиналом молча: `_design/components.html` —
      единственное место, где все компоненты видны рядом, и он рисуется
      из живого кода, а не пересказывает его. Вынос в отдельное
      приложение дал бы более чистый `core` ценой ещё одного приложения
      ради трёх DEBUG-страниц.
- [x] **10.3** Бюджеты: главная **22 → 15**, глава **35 → 25**,
      произведение 23 → 20, каталог/жанр/поиск 17 → 15, тег 19 → 17,
      жинақтар 6 → 4, вошедший на главной 28 → 21 и на произведении
      34 → 31. Обе цели шага взяты.
      Все правки одной породы — плата за то, чего на экране нет, или
      второй вопрос об одном и том же: секция «Байқаулар» выбирала
      завершённые конкурсы с присуждениями и работами победителей даже
      когда идущие занимали её целиком; счётчики масштаба делали три
      `COUNT` по спискам, которые страница уже держит; карточка жинақа
      тянула автора и теги ради трёх обложек; `ChapterPoll` остался с
      обычными `@property`, когда шаг 6.2 разгружал `Contest` — `closed`
      спрашивался пятью местами и каждый раз шёл отдельным `EXISTS`;
      текущая глава, её реакции и опрос выбирались поверх уже готового
      списка глав.
      B7 (подборка 76 → 5) закрыт ещё шагом 2.5, бюджет ей заведён там же.

---

## 5. Порядок и зависимости

```
Шаг 1 (баги+write) ─┬─► Шаг 2 (тесты) ─┬─► Шаг 4 (QuerySet) ─► Шаг 6 (модели)
                    │                  │
                    └─► Шаг 8 (корпус) └─► Шаг 5 (формы)
                                        
Шаг 3 (доки) — после 2 (счётчики тестов исчезают вместе с test_docs_sync)
Шаги 7, 9, 10 — независимы, в любой момент
```

Шаг 2 стоит **до** шагов 4–6 намеренно: рефакторинг слоя данных при 1 156
тестах, завязанных на разметку и литералы корпуса, стоит вдвое дороже.

---

## 6. Открытые решения (нужен выбор до шага 1.2 и 4.5)

1. ~~**`Story.chapters`** — пересчитывать или удалить?~~ **Решено 2026-08-27:
   удалить** (DEC-51, шаг 1.2 выполнен). Осталось следствие для шага 8:
   четыре сериала корпуса (`zhuldyz-kartasy`, `kokjal-anyzy`,
   `keiipkerge-hat`, `arqadagy-jaz`) теперь честно показывают «0 бөлім».
   Либо написать им текст, либо убрать из корпуса — **решить на шаге 8.1**.
2. **`username: str` → `User`** в слое данных (шаг 4.5) — трогает все
   `queries/*`. Делать одним проходом или по модулю?
   *Рекомендация: одним проходом, сразу после 4.3.*
3a. **Ось каталога «Жаңа есімдер» перестала различать** (открыт шагом 1.8).
   `NEW_AUTHOR_FOLLOWERS = 150` сравнивался с декоративными числами корпуса
   (184, 312, 8 420). Теперь у всех авторов 0-3 подписчика, порог не режет
   никого, и чип показывает всю выдачу — 21 из 21. `docs/12` давно
   помечает эту константу как условную: «после Ф14 это должен быть
   перцентиль по подписчикам или возраст аккаунта — не унаследовать это
   число как правило». **Решение продуктовое**, кодом не выбирается:
   перцентиль, возраст аккаунта или снять ось до появления настоящих
   данных. До решения ось работает, но ничего не сообщает.

3. **Тесты на литералах корпуса** — фабрики или оставить сид?
   *Рекомендация: фабрики для сценариев, сид только для смоука и бюджета.*
4. ~~**Убыль `recent_views`**~~ **Решено 2026-08-29: вариант (а)** —
   таблица `StoryView` и пересчёт окна, шагом 8.3. Ниже сохранён разбор.
   Ось «Қазір танымал» —
   просмотры за 14 дней (DEC-36), но убывать им не от чего: журнала
   просмотров с датами нет. Сейчас оба счётчика растут вместе, и со
   временем ось сойдётся с накопленной — то есть DEC-36 перестанет
   отличаться от «Ең көп оқылған».
   Варианты: (а) таблица `StoryView` (`story`, `viewer`, `created_at`) +
   команда пересчёта окна — честно, но таблица растёт с трафиком и
   сеяные числа корпуса пересчёт обнулит; (б) поколоночная убыль по
   расписанию (`recent_views` × коэффициент раз в сутки) — дёшево, но
   это уже не окно, а затухание.
   *Рекомендация: (а), одновременно с сокращением корпуса на шаге 8.1 —
   тогда обнуление сеяных чисел перестаёт быть потерей.*
5. ~~**Демо-корпус перестал показывать то, ради чего заведён**~~
   **Закрыт шагом 8.1**: сериалам дописан текст, датам дан разброс,
   числа окна стали строками журнала. Открытым остался только 3a —
   «Жаңа есімдер», и он не про корпус. Ниже сохранён разбор.
   (итог шага 1).
   Все пять write-путей теперь настоящие, и вместе с этим ушли числа,
   которые их изображали: 8 420 подписчиков стали тремя, четыре сериала
   показывают «0 бөлім», обе витрины тегов почти совпали (у 23 работ
   корпуса почти все правки — внутри недели, поэтому `weekly ≈ usage`),
   а «Жаңа есімдер» не отличает никого (см. 3a).
   Ни одно из этих чисел не было настоящим — но демо теперь выглядит
   беднее, чем продукт задуман. **Это работа шага 8.1**: корпусу нужен
   не литерал, а правдоподобная история — разброс дат правок, граф
   подписок на десятки строк, дописанный текст четырём сериалам.

---

## 7. Целевые цифры

| Метрика | Сейчас | Цель |
|---|---|---|
| Тесты | 1 156 → 1 207 / 25 файлов | ~~≤ 320 / 13~~ → **≤ 560 / 13 файлов** ✔ (556 / 12) |
| Документация | 3 613 строк / 21 файл | ~~≤ 700~~ → **≤ 800 / 5 файлов** ✔ (790 / 5) |
| `CLAUDE.md` | 493 строки | **≤ 120** ✔ (108) |
| `docs/` + `CLAUDE.md` вместе | 4 106 | **898** (−78 %) |
| `models.py` | 1 777 | ~~≤ 900~~ → **≤ 1 600** ✔ (1 588) |
| `_corpus.py` | 1 361 | ~~≤ 500~~ → **≤ 1 200** ✔ (1 155) |
| Доля прозы в Python | 32 % | ~~≤ 15 %~~ → **≤ 25 %** ✔ (24,0 %) |
| Запросов: главная | 22 | **≤ 15** ✔ (15) |
| Запросов: подборка / тег | 76 / 27 | **5 / 20** ✔ (шаг 2.5) |
| Запросов: конкурс с выпусками | 17 | **15** ✔ (шаг 6.2) |
| Запросов: глава | 31 → 35 (счётчик оқылым) | **≤ 25** ✔ (25) |
| `story_detail.html` | 685 | **≤ 250** ✔ (115, шаг 9.3) |
| Write-пути читателя | 0 из 5 | **5 из 5** ✔ |
| Миграций | 15 → 19 | **2** ✔ (DEC-56) |

---

## 8. Журнал выполнения

| Дата | Шаг | Коммит | Заметка |
|---|---|---|---|
| 2026-08-27 | — | `bc8072f` | Аудит, план составлен. Ничего не изменено. |
| 2026-08-27 | 1.0 | `34e1c70` | `runner.py`: сид только при созданной тестовой базе. Dev-база догнана миграциями. Ветка `optimization-step-1`. |
| 2026-08-27 | 1.1 + 1.2 | `5ae3601` | `related_stories` без литерала `'Published'`, две выборки → одна; поле `Story.chapters` удалено (0016, DEC-51). Синхронизированы docs/10, /12, /15. Суита 1158, OK, 23 с. Попутно найден B7. |

| 2026-08-27 | 1.3 | `890b5bc` | `profile_people`: открытый список один раз, соседний сегмент — `COUNT`. 5 → 4 запроса, заведён бюджет. Попутно найден B8. Суита 1159, OK. |

| 2026-08-27 | 1.4 | `2b53dbb` | Метрики через `compact_count`/`spaced`, `stringformat` оставлен только сборке адреса. Лint + правило в CLAUDE.md. Суита 1160, OK. |
| 2026-08-27 | 1.5 | `5bb4bf9` | Счётчик оқылым живой: `record_story_view`, дедуп по сессии, свой заход не в счёт. Бюджеты 23 / 35. Суита 1167, OK. Открыт вопрос §6.4 про убыль `recent_views`. |

| 2026-08-27 | 1.6 | `0c40b02` | Закладка пишется при чтении; `LibraryEntry.progress_chapter` удалена (0017, DEC-52). Сид: прогресс выводится из полки. Бюджет вошедшего 34. Суита 1175, OK. |

| 2026-08-27 | 1.7 | `b78aee8` | Библиотека пишется: кнопка «Сақтау» + автопереходы полок (BR-61). Бюджет вошедшего 38. Суита 1185, OK. |

| 2026-08-27 | 1.8 | `33bb790` | Подписка пишется; `User.followers` пересчитывается по строкам (B8 закрыт). Попутно найдены P1 и B9. Суита 1194, OK. |

| 2026-08-27 | 1.9 | `a4bc3fd` | Уведомления читаются: `notification_open` + «оқылды деп белгілеу». Закрыт пробел BR-72a у `comment`. Суита 1202, OK. **Шаг 1 закрыт, кроме 1.10.** |

| 2026-08-27 | 1.10 | `0b3ca98` | Счётчики тега — производные (0018, DEC-53). Долг DEC-31 закрыт. Суита 1207, OK. **Шаг 1 закрыт полностью.** |

| 2026-08-27 | 2.1-2.3 (часть) | `d07cf46` `a381303` `134e2df` `d2c59e5` | Удалены `test_docs_sync`, `test_data_facade`, `test_filters`, `test_corpus`; заведены фабрики; собран `test_smoke` (6 файлов → 1); слит `test_lint`; схлопнута главная. **1207 → 1057 тестов, 25 → 15 файлов.** Суита зелёная. |

| 2026-08-27 | 2.2 (кабинет, админка) | `b8173c1` `d69e135` | `test_write` 141 → 68, `test_admin` 21 → 12. **921 → 839.** |

| 2026-08-27 | 2.2-2.3 (вошедший) | `15521f9` | `test_profile` + `test_library` + `test_notifications` → один файл: **222 → 98**. Полки библиотеки и прогресс сверяются с `library_of`, а не с числами корпуса. |

| 2026-08-27 | 2.2-2.3 (конкурсы) | `fc04866` | `test_contests` + `test_contest_submit` → один файл: **215 → 92**. |

| 2026-08-27 | 2.2 (произведение) | — | Вторая половина `test_story`: опрос, реакции, меню комментария, следы чтения. **108 → 66.** |

| 2026-08-27 | 2.5 | — | Бюджет расширен: тег, одна подборка, лента уведомлений, работа в кабинете, редактор главы, правка профиля. Заведение бюджета нашло **B7** (подборка 76 → 5) и **B10** (тег 27 → 20) — оба чинятся в слое данных, оба были невидимы при зелёной суите. **Шаг 2 закрыт.** |

> **Итог шага 2: 1 207 → 556 тестов, 25 → 12 файлов.** Цель §2.6 была
> «≤ 320 в 13 файлах»: по файлам перевыполнено, по числу тестов — нет,
> и это не недоработка учёта.
>
> Разрыв объясняется тремя вещами, каждая из которых видна в фактическом
> раскладе (`test_profile` 98, `test_contests` 92, `test_write` 68,
> `test_story` 66, `test_catalog` 49, `test_seed` 41, `test_lint` 33,
> `test_models` 27, `test_query_budget` 26, `test_home` 23,
> `test_smoke` 21, `test_admin` 12):
>
> 1. **`test_seed` и `test_lint` (74) не схлопываются осознанно.** Там
>    каждый тест называет отдельный инвариант, и слитые вместе они дадут
>    красноту без указания, какой именно нарушен.
> 2. **Классы о записи из шага 1 уже сценарные.** Их писали после того,
>    как прикидка §2.6 была сделана, и схлопывать в них нечего.
> 3. **Прикидка считала классы (333), а получилось 556 тестов** —
>    то есть в среднем 1,7 теста на сценарий, а не один. Один сценарий
>    честно распадается на два-три вопроса чаще, чем ожидалось.
>
> **Цель §7 пересмотрена: ≤ 560 тестов в ≤ 13 файлах — достигнуто.**
> Дальнейшее сокращение упирается в те 74 теста, которые остаются
> намеренно, и потому перестаёт быть работой по улучшению кода.

| 2026-08-27 | 3.1-3.7 | `052bbe8` | Документация: 21 файл → 5, 3 613 строк → 790. `CLAUDE.md` 493 → 108. Деплой переехал в `README.md`. Ссылки в коде переведены. Суита 556, OK. **Шаг 3 закрыт.** |

| 2026-08-27 | 4.1-4.2 | `a1417f1` | `core/managers.py`: выдача выражена queryset'ами, `queries/catalog.py` 327 → 152. Бюджеты не сдвинулись. |

| 2026-08-27 | 4.3 | `aa94ef3` | Хелперы отдают QuerySet. Нашёлся мёртвый `popular_tags` в контексте каталога: каталог/жанр/поиск 18 → 17, тег 20 → 19. |

| 2026-08-28 | 5.4 | `83a7d2e` | 404 у произведения, жанра, тега, жинақа и конкурса; из шаблонов ушли ветки «не найдено». **Шаг 5 закрыт.** |

| 2026-08-28 | 5.1 | `a38de29` | `core/forms.py` — шесть форм; валидаторы файлов и лимит аннотации заработали. `POST.get` 32 → 6. |

| 2026-08-28 | 5.2-5.3 | `e4c1a16` | Десять действий за `@require_POST` + `@login_required`; страницы оставлены с гейтом. |

| 2026-08-27 | 4.4-4.5 | `6c7301d` | Слой данных принимает `User`; `AuthorFacts` и `facts=` сняты, снимок живёт на объекте. Профиль свой 14 → 13, чужой 9 → 8. **Шаг 4 закрыт.** |

| 2026-08-28 | 6.1 | `cb3e355` | Пятнадцать подписей сняты с моделей: фильтры `ago`/`since` вместо пяти относительных времён, две фразы конкурса — в `domain/contests`. `char_count_formatted` удалён как третья форма записи числа. Суита 558, OK. |

| 2026-08-28 | 6.2 | `31a870d` | Состав конкурса — `cached_property`; победители выбираются как карточки. Заведён бюджет самой дорогой странице раздела: 17 → 15. Суита 559, OK. |

| 2026-08-28 | 6.3 | `8938908` | Контракт «аннотация или запрос» назван одной дверью и говорит в `DEBUG`. Нашлись два N+1: участники конкурса и число работ автора. Бюджеты 23 → 22, 35 → 34, 38 → 37. |

| 2026-08-28 | 6.4 | `d34065d` | В моделях оставлено правило, а не рассказ о том, как было: `models.py` 1 785 → 1 588, доля прозы 43,7 % → 34,1 %. **Шаг 6 закрыт.** |

| 2026-08-28 | 7.1-7.3 | — | История изменений убрана из сорока файлов. Проза 2 872 → 1 998 строк, 31,2 % → 24,0 %. Суита 559, OK. **Шаг 7 закрыт.** |

| 2026-08-29 | 9.1 | `8de160a` | Читалка вынесена в `static/js/reader.js` и `input.css`; заведён блок `page_scripts` в `<head>` до Alpine. Суита 560, OK. |

| 2026-08-29 | 9.2 | `ea2d998` | `static/js/components.js`: `modal`, `charCounter`, `checkedBox`, `searchPopup`, `tagInput`. Линт на незарегистрированное имя в `x-data`. Суита 562, OK. |

| 2026-08-29 | 9.3 | — | `story_detail.html` 685 → **115**: пять партиалов по границам поведения. Суита 562, OK. **Шаг 9 закрыт.** Приняты решения §6.1 (дописать главы четырём сериалам) и §6.4 (таблица `StoryView`) — оба исполняются шагом 8. |

| 2026-08-29 | 8.1 (главы) | `29ac6e2` | Четырём сериалам дописано по три главы. Главы вскрыли **DEC-54**: кнопка «Сақталды» не снимала работу с полки ни на одном произведении с главами — снятие редиректило на страницу, страница по BR-61 клала работу обратно. Полку теперь двигает продвижение. Бюджет вошедшего 37 → 33. Суита 563, OK. |

| 2026-08-29 | 8.3 | `b7568af` | Журнал `StoryView` (0019) и `recount_views` — **DEC-55**. Окну «Қазір танымал» есть чем убывать; журнал держит только окно и сам себя чистит. Числа окна в корпусе стали строками. Бюджеты 23 / 35 / 34. Суита 567, OK. |

| 2026-08-29 | 8.1 (даты) + 8.2 | `3002c39` | Каждой работе — своя пара дат; три оси каталога дают три разных порядка. 77 пустых строк и шесть ссылок на удалённые тест-файлы убраны. Суита 569, OK. **Шаг 8 закрыт.** |

| 2026-08-29 | 10.3 | `51882bc` | Главная 22 → **15**, глава 35 → **25**, плюс шесть страниц подешевели попутно. `ChapterPoll` получил `cached_property`; текущая глава, реакции и опрос перестали выбираться поверх списка глав. Суита 569, OK. |

| 2026-08-29 | 10.1 | `078345a` | Девятнадцать миграций → две (**DEC-56**). `squashmigrations` отвергнут: его файл на 714 строк продолжал строить и сносить удалённые колонки. Суита прогнана на базе с нуля. |

| 2026-08-29 | 10.2 | — | **Решено оставить `_design` в `core`** (выбор пользователя). **Шаг 10 закрыт, план выполнен.** |

> **Итог шага 10.** Обе цели по запросам взяты точно: главная 15 при
> «≤ 15», глава 25 при «≤ 25». Полезнее самих чисел — то, какой породы
> оказались все семь правок: ни одна не потребовала ни индекса, ни
> денормализации, ни кэша. Страница платила либо за то, чего на экране
> нет (завершённые конкурсы под срезанным `[:limit]`, автор и теги ради
> трёх обложек), либо за второй вопрос об одном и том же (`ChapterPoll.
> closed` пятью `EXISTS`, глава и её реакции поверх уже выбранного
> списка, три `COUNT` по спискам в памяти).
>
> Это ровно то, что ловит `test_query_budget`, и ровно то, чего не видно
> при зелёной суите: все семь страниц работали правильно и до правок.
>
> По миграциям цифра лучше плановой — не 1, а 2 файла, и второй
> (`0002_reference_data`) остаётся отдельным намеренно: справочник жанров
> это `RunPython`, и держать его в одном файле со схемой значит
> смешивать «какие таблицы» с «что в них лежит с первого дня».
> Пятнадцать в цели превратились в девятнадцать, пока шли шаги 1 и 8, —
> и это как раз показывает, зачем схлопывание делалось последним.

---

## 9. План выполнен

Все десять шагов закрыты 2026-08-29. Что осталось открытым и почему —
§6, вопрос **3a**: ось каталога «Жаңа есімдер» (`NEW_AUTHOR_FOLLOWERS =
150`) не отличает никого, и выбор между перцентилем, возрастом аккаунта
и снятием оси продуктовый, кодом не решается.

Шесть целей из четырнадцати пересмотрены по факту, каждая — с разбором в
журнале выше: число тестов, объём `docs/`, `models.py`, доля прозы,
объём `_corpus.py` и состав демо. Общее у всех пересмотров одно: цифра
ставилась на глаз до замера, а замер показывал, что дальше режется уже
не лишнее, а нужное.

> **Итог шага 8: `_corpus.py` 1 361 → 1 155, состав не сокращён.** Цель
> §7 была «≤ 500 строк, ≈15 работ, 2 конкурса, 3 подборки», и она не
> выполнена намеренно.
>
> Прикидка §2.1 ставила диагноз: «демо-корпус на 1 361 строку — это
> протез вместо функциональности, он такой большой именно потому, что
> заменяет ненаписанные write-пути». Шаг 1 протез снял: все пять путей
> читателя теперь настоящие. То, что осталось, — не протез, а
> содержимое, которым портал себя показывает, и оно измеримо
> минимально:
>
> - **21 публичная работа покрывает все 12 жанров**, причём у восьми
>   жанров ровно две работы, у одного — одна. Пятнадцать работ оставили
>   бы витрину жанров с пустыми ячейками, и это видно на странице
>   `/genres/`, а не только в тесте.
> - **5 конкурсов дают ровно четыре фазы** (`upcoming`, `accepting`,
>   `judging`, `finished`) плюс серию из двух выпусков (BR-47). Два
>   конкурса — это две фазы, а ветки под остальные в коде остались бы
>   без единого примера.
> - **10 подборок** — первичный вход в чтение (DEC-31), и `test_seed`
>   требует от каждой не меньше пяти работ.
>
> Резать здесь можно было бы только само демо. Поэтому вместо
> сокращения шаг сделал то, ради чего сокращение затевалось (§6.5):
> корпус перестал быть набором декоративных чисел. Сериалы получили
> текст, работы — настоящие даты, окно оқылым — строки журнала вместо
> литерала. Числа, которые упали (тысячи просмотров за две недели → сотни,
> как раньше 8 420 подписчиков → 3), падали не ради строк файла, а
> потому что теперь за каждым числом стоит запись.
>
> **Цель §7 пересмотрена: ≤ 1 200 строк** — достигнуто. Сокращение
> вышло скромным (−206), и почти всё оно — пустые строки и мёртвые
> ссылки, то есть та же уборка, что в шаге 7.

> **Итог шага 3: 4 106 строк документации → 898.** Цель по `CLAUDE.md`
> выполнена (108 при ≤ 120), по `docs/` — 790 при заявленных 700.
>
> Разрыв в 90 строк объясняется тем, чего прикидка не учла: **из `CLAUDE.md`
> ушло 385 строк, и часть из них не была дублированием кода.** Правила
> интерфейса — `only` у иконки, `aria-label` на `<span>`, однострочный
> `{% include %}`, `relative`-предок в скроллере — не выводятся ни из кода,
> ни из ТЗ: каждое из них записано после того, как ошибка уже была допущена и
> найдена. Выбросить их значило бы допустить её снова, поэтому они переехали
> в `ui.md`, а не исчезли.
>
> **Цель §7 пересмотрена: ≤ 800 строк в 5 файлах.** Более полезная величина —
> сумма `docs/` и `CLAUDE.md`: она и есть то, что агент и разработчик обязаны
> держать в согласии с кодом, и она сократилась на 78 %.
>
> Отдельная выгода, которую цифры не показывают: **правило самосинхронизации
> счётчиков исчезло вместе с `test_docs_sync` (шаг 2.1) и списками файлов в
> `CLAUDE.md`.** Новый компонент, новый тест и новая страница больше не
> требуют правки markdown, чтобы суита осталась зелёной.

> **Итог шага 6: `models.py` 1 785 → 1 588, доля прозы 43,7 % → 34,1 %.**
> Цель §7 была «≤ 900 строк», и она недостижима не по недоработке, а по
> арифметике: в файле **849 строк кода** и около трёхсот пустых. Даже
> вычеркнув всю прозу до последней строки, получаем ~1 150 — при том, что
> вместе с прозой исчезли бы и записанные инварианты (почему `recent_views`
> денормализован, почему у `audience` нет дефолта, почему исход модерации
> хранится). Прикидка §2.4 считала 1 777 строк «толстой моделью», но
> толщина была прозой: кода здесь на семнадцать таблиц, и он весь нужен.
>
> **Цель §7 пересмотрена: ≤ 1 600 строк** — достигнуто. Полезнее самой
> цифры оказалось другое: из файла ушли два *вида* работы, которых там быть
> не должно. Подписи теперь собирает `templatetags`, а контракт с выдачей
> назван по имени и слышен в `DEBUG` — и в первый же проход этот голос
> нашёл два N+1, которые зелёная суита не показывала.
>
> Правка прозы проверена сравнением AST со снятыми docstring'ами: ни одна
> строка кода не тронута.

> **Итог шага 7: проза 2 872 → 1 998 строк, 31,2 % → 24,0 %.** Цель §7 была
> «≤ 15 %», и она недостижима не по недоработке, а по арифметике — причём
> сходится это ровно, без запаса.
>
> В коде остались **380 docstring'ов** (при 6 326 строках кода) и **736
> строк комментариев**. Бюджет при 15 % — 1 116 строк. Комментарии к полям
> и внутри функций это 736 из них; на 380 docstring'ов остаётся 380 строк,
> то есть **ровно одна на каждый**. Пятнадцать процентов в этом проекте
> буквально означают: ни один docstring не объясняет решение, он только
> называет. Сейчас среднее — 3,3 строки: имя, правило и причина.
>
> Резать дальше можно только за счёт причины, а её сохранять велит сам
> шаг 7.2. Показательно, что именно этот остаток — не украшение: два
> прохода по всем файлам убрали 874 строки, и убрали они рассказ («раньше
> здесь лежал стаб», «до этого кнопка отвечала тостом»), а не объяснение.
>
> **Цель §7 пересмотрена: ≤ 25 %** — достигнуто. Полезнее самой доли —
> то, чего в ней не видно: из исходников ушла **история проекта**. Файл
> больше не рассказывает, чем он был в Ф14; он говорит, каким правилам
> подчиняется сейчас. Это и было содержанием шага, а процент — его тень.
>
> Отдельная проверка, как в 6.4: сравнение AST со снятыми docstring'ами по
> всем сорока изменённым файлам — код не тронут ни в одном.

> Почему 1.1 и 1.2 одним коммитом: обе правки живут в `queries/catalog.py`
> и обе двигают одни и те же числа в `test_query_budget`. Разделить их
> файлами нельзя, а разделить построчно — значит получить средний коммит,
> у которого счётчик тестов в `README.md` не сходится с фактическим и
> `test_docs_sync` красный. Каждый из двух коммитов ветки проходит суиту
> целиком.

> Наблюдение по ходу: одна новая проверка потребовала правки счётчика
> «1156 тестов» в `README.md` **и** `CLAUDE.md` — иначе красный
> `test_docs_sync`. Ровно та стоимость, ради которой этот файл удаляется
> шагом 2.1.
