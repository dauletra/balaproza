"""Запросы к моделям: то, чем `stub_data` был для чтения.

Слой существует, чтобы views не собирали `QuerySet` руками. Причина не
в чистоте: правила выдачи — «в каталог идут только публичные статусы»
(DEC-23), «ось жаса накопительная» (DEC-38), «дефолт сортировки — окно в
14 дней» (DEC-36) — обязаны лежать в одном месте, иначе каждая страница
поймёт их по-своему. Ровно это и случилось до Ф14 со сборкой URL
каталога в шаблонах.

Наружу всё отдаётся через фасад `core.data`; напрямую сюда ходить не
нужно, и по мере миграции строки в фасаде переезжают из `stub_data`
именно в эти модули.
"""
