"""Запросы к моделям: слой, в котором views не собирают `QuerySet` руками.

Причина не в чистоте: правила выдачи — «в каталог идут только публичные
статусы» (DEC-23), «ось жаса накопительная» (DEC-38), «дефолт сортировки —
окно в 14 дней» (DEC-36) — обязаны лежать в одном месте, иначе каждая
страница поймёт их по-своему.

Наружу всё отдаётся через фасад `core.data`; напрямую сюда ходить не нужно.
"""
