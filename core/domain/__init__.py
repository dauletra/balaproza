"""Домен: то, что не является данными и потому переживёт Ф14.

`core/stub_data.py` держал вперемешку две разные вещи: сами данные
(произведения, авторы, конкурсы) и словарь предметной области — оси
каталога, закрытый список реакций, ступени наград, подписи статусов,
формулировки времени. Первое заменилось таблицами, и файла больше нет.
Второе никуда не делось: это правила, а не записи, и хранить их в модуле
с именем «стаб» значило однажды удалить его вместе с ними.

Правило одно: **домен ничего не знает о хранилище**. Отсюда нет импорта
ни моделей, ни `core.data`, ни демо-корпуса — иначе получается цикл, а
константы нельзя использовать в миграциях и в админке. Всё, что здесь
лежит, — чистые значения и чистые функции.

Предикаты над произведением (`KIND_PREDICATES`) типизированы по утиному:
им нужен объект с `format` и `status`. Ось каталога поэтому пережила
замену хранилища, не изменившись ни в одном символе, — это не
случайность, а то, ради чего слой заведён.
"""

from .awards import *          # noqa: F401,F403
from .catalog import *         # noqa: F401,F403
from .contests import *        # noqa: F401,F403
from .formatting import *      # noqa: F401,F403
from .notifications import *   # noqa: F401,F403
from .story import *           # noqa: F401,F403
