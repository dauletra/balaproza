"""Конкурс: фазы, подписи результата и заметки к подаче.

Фаза выводится из трёх дат (DEC-45), но её имена и подписи — словарь
предметной области: их показывают бейдж, рейл и уведомление, и собирать
слово в шаблоне запрещено ровно так же, как у статусов работы (BR-40).

`SUBMISSION_NOTES` — заметки, а не запреты (BR-24): форма ничего не
отклоняет, решение о работе принимает человек.

Две фразы конкурса — возрастное требование и «что дальше и когда» — тоже
живут здесь, а не свойствами `Contest`: их спрашивает не только шаблон,
но и чек-лист подачи, и проверка формы. Функция от дат и чисел вызывается
из обоих мест; свойство модели — только из одного.
"""

from datetime import date

from .formatting import kk_date

# Фазы конкурса (BR-40, DEC-45). Порядок — хронологический.
CONTEST_PHASES = ("upcoming", "accepting", "judging", "finished")

CONTEST_PHASE_LABELS = {
    "upcoming":  "Жақында",
    "accepting": "Өтінім қабылдау",
    "judging":   "Қазылар қарауда",
    "finished":  "Аяқталды",
}

# Семантика бейджа фазы — та же шкала, что у статусов произведения.
CONTEST_PHASE_BADGE = {
    "upcoming":  "info",
    "accepting": "published",
    "judging":   "attention",
    "finished":  "neutral",
}


# Путь заявки: подана — рассмотрена — решение (BR-41). Победа сюда не
# входит: она приходит присуждением награды (DEC-46), а не сменой статуса,
# потому что «первое место» — акт жюри, а не состояние документа.
SUBMISSION_STATUSES = ("reviewing", "accepted", "rejected")

# Ответ формы подачи на вопрос об AI-помощи (DEC-21). Пишется один раз при
# создании заявки, дальше поле read-only — автор его не редактирует.
AI_DECLARATIONS = ("no", "partial", "yes")


# Подписи результата — те же слова, что в «Менің өтінімдерім» (BR-41),
# плюс «Жеңімпаз» для победы: победа выводится из `Contest.winners`,
# отдельного статуса заявки под неё нет.
CONTEST_RESULT_LABELS = {
    "winner":    "Жеңімпаз",
    "accepted":  "Қабылданды",
    "reviewing":  "Қаралуда",
    "rejected":  "Қабылданбады",
}

# Что из результата видит посторонний (BR-74a). Всё остальное публично
# выглядит просто участием.
PUBLIC_CONTEST_RESULTS = ("winner", "accepted")


# Что стоит знать автору о работе перед подачей (BR-24). Это **заметки,
# а не запреты**: форма ничего не отклоняет.
#
# Раньше эти же ключи гасили радио и кнопку отправки, то есть отказывали
# от имени конкурса до всякого жюри. Решение о работе принимает человек,
# а не чек в браузере: короткий текст может быть намеренно короткой
# формой, а работа, уже поданная в другой конкурс, — предметом
# разбирательства, а не поводом молча закрыть дверь.
SUBMISSION_NOTES = {
    "too_short": "Көлемі шарттан аз",
    "too_long":  "Көлемі шарттан үлкен",
    "busy":      "Бұл шығарма басқа байқауда тұр",
}


def eligibility_line(min_age, max_age) -> str:
    """Возрастное требование словами. Пусто — конкурс его не ставит (BR-48).

    Собирать эту строку в шаблоне запрещено: её показывают чек-лист
    подачи, секция условий и чекбокс подтверждения.
    """
    if min_age and max_age:
        return f"{min_age}-{max_age} жас"
    if min_age:
        return f"{min_age} жастан бастап"
    if max_age:
        return f"{max_age} жасқа дейін"
    return ""


def timing_line(phase: str, opens_on: date, closes_on: date,
                results_on: date) -> str:
    """«Что дальше и когда» одной строкой. У завершённого — пусто.

    Спрашивают об этом из трёх мест сразу: строка заявки, конкурсное
    уведомление и рейл. Отсчёта в днях здесь нет — он протухает назавтра
    (BR-40a).
    """
    if phase == "upcoming":
        return f"Қабылдау {kk_date(opens_on)} басталады"
    if phase == "accepting":
        return (f"Қабылдау {kk_date(closes_on)} жабылады · "
                f"жеңімпаздар {kk_date(results_on)} жарияланады")
    if phase == "judging":
        return f"Жеңімпаздар {kk_date(results_on)} жарияланады"
    return ""
