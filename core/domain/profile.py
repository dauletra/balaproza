"""Профиль: справочник формы редактирования (Ф15, Этап 6).

`GENDERS` — закрытый список, тот же приём, что `AI_DECLARATIONS` у
конкурса: view сверяет присланное значение с этим списком, а не
пропускает произвольную строку в базу.
"""

GENDERS = ("boy", "girl")

# Подписи покрывают оба возраста одним значением: авторы платформы — не
# только дети (DEC-84), а заводить четыре отдельных значения ради двух
# слов было бы лишней осью данных.
GENDER_LABELS = {
    "boy":  "Ұл/Ер",
    "girl": "Қыз/Әйел",
}
