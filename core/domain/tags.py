"""Теги: состояния UGC-таксономии (docs/11, DEC-26).

Тег заводит автор, а не редакция, поэтому у него есть путь: `pending` —
создан и ждёт модератора, `accepted` — попал в автокомплит и в публичные
витрины, `rejected` — снят. Порядок значим: он же порядок пути.

Pending виден автору работы и скрыт от публики (BR-TAG-07) — правило про
видимость живёт в запросах, здесь только сам словарь.
"""

TAG_STATUSES = ("pending", "accepted", "rejected")
