# 10 · Реестр принятых решений

> `Обновлён: 2026-09-03` · `Сверен с кодом: 21a4bcd`

Сводная таблица всех несогласованностей и открытых вопросов прототипа/аудитов, закрытых в этом ТЗ. Каждое решение — `DEC-NN`.

| # | Что было (проблема / несогласованность) | ✅ Принятое решение | Где описано |
|---|------------------------------------------|---------------------|-------------|
| **DEC-01** | Неясно, как пользователь становится автором | Отдельной роли «Читатель» нет. Любой авторизованный пользователь автоматически считается Автором сразу после регистрации (доступ к авторскому кабинету, библиотеке, лайкам, комментариям и созданию произведений включён по умолчанию) | [architecture.md](architecture.md) |
| **DEC-02** | Аудиты: платформа гонит трафик в соцсети vs MVP-упрощение | MVP: чтение/произведения/конкурсы — на платформе; «Авторлар мектебі» (видео, вебинары, интервью, курсы) — внешние ресурсы, на сайте только блок ссылок | [architecture.md](architecture.md) |
| **DEC-03** | Прототип на абсолютном позиционировании (px) | В продакшене — поток flex/grid, резиновая сетка; mobile/desktop = состояния одной верстки; +tablet 768px | [architecture.md](architecture.md) |
| ~~**DEC-04**~~ | Babel-in-browser, UMD-React, `window.*`, CDN-шрифты | ~~Сборщик (Vite/Next), ES-модули, слой данных~~ — **отменено DEC-29**. В силе остались только self-hosted шрифты и единый источник токенов в CSS-переменных | [architecture.md](architecture.md) |
| **DEC-05** | Токены продублированы: CSS-vars + инлайн `rgb()` | Единый источник — CSS-переменные; инлайн-цвета запрещены | [ui.md](ui.md) |
| **DEC-06** | Два цвета действия: бирюзовый (desktop) и синий (mobile FAB) | Единый accent — бирюзовый `--color-brand`; синий — только информационный семантический | [ui.md](ui.md) |
| **DEC-07** | Счётчик непрочитанных — разные места | Единый красный `rgb(220,38,38)`. После DEC-25 и пересмотра mobile nav точка показа осталась одна — колокольчик в хедере | [ui.md](ui.md) |
| **DEC-08** | Промо-баннер выбивается из палитры (жёлто-зелёный + красный) | Намеренный сигнальный акцент; вынесен в токены `--color-promo-*`, только для конкурсов | [ui.md](ui.md) |
| **DEC-09** | Шрифт Caveat в проде; иконки смешивают fill/stroke | Caveat исключён (3 шрифта + Georgia); иконки — единый outline, залитые только для активных состояний | [ui.md](ui.md) |
| **DEC-10** | Disabled-состояния не проработаны | Обязательны для всех кнопок и полей | [ui.md](ui.md) |
| **DEC-11** | Состав жанров и их назначение произведению | Закрытый справочник 12 жанров; 1 основной + до 1 доп. | [ui.md](ui.md), [spec.md](spec.md) |
| **DEC-12** ⛔ | OKLCH не поддерживается старыми браузерами | Добавить sRGB-fallback через `@supports`. **Не выполнено:** в `input.css` нет ни одного `@supports`, ролей стало 15 вместо 3 (реальный объём — 180 значений, а не 36). Требуется решение: сузить набор ролей, отказаться от fallback и поднять NFR-40, либо перенести жанровую тему в CSS-переменные. NFR-41 не выполнен | [ui.md](ui.md) |
| **DEC-13** | Валидация форм не системна | Единые InputValidated/TextAreaValidated: default/success/error + aria-describedby | [ui.md](ui.md) |
| **DEC-14** | Два компонента жанра: GenrePill (текст) и GenreChip (цвет) | Остаётся только GenreChip (OKLCH); GenrePill исключён | [ui.md](ui.md) |
| **DEC-15** | «Tabs that don't tab»: декоративные табы, всё отрендерено сразу (Story, Profile, mobile) | Анти-паттерн исключён; sticky scrollspy или честное переключение | [spec.md](spec.md), [ui.md](ui.md) |
| **DEC-16** | Множество версий экранов (Home v1-v3, Story v1, и т.д.) | Каноничны только финальные версии; эксперименты в разработку не идут | [spec.md](spec.md) |
| **DEC-17** | Loading/Error состояния отсутствовали | Обязательны для всех data-зависимых экранов | [ui.md](ui.md) |
| **DEC-18** | Правый рейл отдавал функции сайта соцсетям | Рейл несёт продуктовые блоки; соцсети убраны в footer | [ui.md](ui.md) |
| **DEC-19** | Соцсети в нескольких местах | Footer + блок «Авторлар мектебі»; контент смотрится вне платформы, видео не встраивается (MVP) | [ui.md](ui.md) |
| **DEC-20** | Жанры — плоская текстовая полоса | Заменены цветными чипами OKLCH | [ui.md](ui.md) |
| **DEC-21** | AI-декларация в подаче на конкурс не финализирована | Обязательный пункт чек-листа; без ответа работа не подаётся | [spec.md](spec.md) |
| **DEC-22** | Школа авторов: страница с уроками vs упрощение MVP | Внутренняя страница школы (уроки/модули/инструкторы/прогресс) исключена; заменена блоком внешних ссылок — модуль LINKS. Аннулированы FR-SCHOOL-01…07 и BR-50…53 | [spec.md](spec.md) |
| **DEC-23** | Инструмент модерации (BR-11) не определён | Для MVP — стандартный **Django admin** (изменение статуса произведения через админку). Кастомный UI модератора (очередь, кнопки Approve/Reject/Request changes, аннотации) отложен на V2. Сам флоу: автор отправляет произведение на проверку → статус `OnModeration` → модератор меняет на `Published`/`NotPublished` с комментарием → автор получает уведомление | [spec.md](spec.md) |
| **DEC-24** | Верификация возраста не определена | **Самодекларация против требования конкурса** (BR-48, уточнено DEC-47): возраст указывается полем `age` при регистрации (FR-AUTH-04) и подтверждается чекбоксом при подаче — но чекбокс называет **вилку этого конкурса**, а не общую, и у конкурса без ценза его нет вовсе. Никаких документов, паспортов или parental consent в MVP — сознательный компромисс ради скорости запуска. Если в эксплуатации обнаружится массовый абьюз — пересмотреть в V2 | [spec.md](spec.md) |
| **DEC-25** | Левый sidebar на десктопе: для гостя содержал 3 декоративные ссылки, дублирующие hero/жинақтар главной | Sidebar **исключён**. В хедере главный вход в чтение — **Оқу** (`/catalog/`), конкурсы остаются отдельной ссылкой **Байқаулар**. Жанрлар — обзорная страница и секция главной, Жинақтар — отдельный editorial-раздел в footer. Личные разделы авторизованного (Кітапхана, Менің шығармаларым, Заявкалар) — через avatar-dropdown в хедере, а mobile-nav авторизованного поднимает Кітапхана. Правый рейл рендерится только если страница явно передала `has_right_rail=True`. Контейнер уменьшен с `max-w-[1440px]` до `max-w-[1280px]` | [ui.md](ui.md) |
| **DEC-26** | Жанровая сетка (12 закрытых категорий) слишком грубая для дискавери — два «фэнтези» могут быть совсем разными произведениями | Введена **UGC-таксономия тегов** параллельно жанрам. До **10 тегов** на произведение; автор создаёт свободно (длина 2-30, kk/ru/lat). Каждый новый тег попадает в статус `pending`, модератор пост-фактум переводит в `accepted` (заходит в автокомплит для следующих авторов) или `rejected`. Часть паттернов в **блок-листе** — создание отклоняется inline. Жанры не меняются — теги их дополняют (second-order навигация). Модерация — в Django admin (DEC-23) | [spec.md](spec.md) |
| **DEC-27** | `genre_detail` и `search_results` — 90% дубликат шаблона/view; планируемый `/tag/<slug>/` стал бы третьим клоном; невозможно комбинировать «жанр + тег» в одной выборке | Каталог унифицируется в **один движок** с тремя branded entry-страницами: `/search/?q=...`, `/genres/<slug>/`, `/tag/<slug>/` (canonical URLs сохраняются для SEO/sharing). Поверх — общий filter bar с chip-фильтрами + общий `_book_list` партиал. Комбинации работают через query-string: `/genres/triller/?tag=mektep`. **Коллекции (Жинақтар) НЕ объединяются** — это editorial curation (статический ручной выбор), отдельный content type. Они переиспользуют только `_book_list`-партиал. «Smart collections» (auto-include по условию) — кандидат на V2 | [spec.md](spec.md) |
| **DEC-28** | Платформа поддерживает и серийные произведения, и цельные рассказы; если ориентироваться только на главы, авторам короткой/средней прозы неудобно публиковаться, а читателям сложно найти быстрое чтение | `Story` остаётся единым content type, но получает `format`: `single` = бір бөлімді шығарма (рассказ/ертегі/эссе/новелла, UI показывает цельный текст без оглавления) и `serial` = көп бөлімді шығарма (главы, оглавление, prev/next). Формат выбирает автор, он не вычисляется автоматически по `chapters == 1`, потому что serial может временно иметь одну главу. Каталог фильтруется по `format` и `length` | [spec.md](spec.md) |
| **DEC-29** | DEC-04 фиксировал целевую архитектуру как сборщик (Vite/Next) + ES-модули + клиентский стор. Проект построен иначе, и ТЗ три месяца описывало продукт, которого нет | **DEC-04 отменяется.** Фактический стек: **Django 6 (серверный рендеринг шаблонов)** + **Tailwind CSS v4 через `@tailwindcss/cli`** (токены в `@theme` внутри `static_src/input.css`) + **Alpine.js 3.14** и **htmx 2.0** как self-hosted `<script>`. JS-сборщика, ES-модулей и клиентского стора **нет и не планируется** в MVP: интерактивность точечная (дропдауны, модалки, автокомплит тегов, quick-search), она не требует SPA. Из DEC-04 в силе остаются два пункта — self-hosted шрифты и единый источник токенов в CSS-переменных | [architecture.md](architecture.md) |
| **DEC-30** | ТЗ описывало три отдельных экрана чтения (`Reading`, `Reading popover`, `Reading dark`); в реализации отдельного маршрута чтения нет | Чтение главы — **инлайн на странице произведения**, глава выбирается через `?chapter=N`. Отдельного маршрута `/story/<slug>/chapter/<n>/` не существует. Причина: у `format=single` оглавления нет вообще, и отдельный экран чтения для него был бы пустой обёрткой; а для `serial` переход на отдельный маршрут терял контекст произведения (аннотацию, теги, комментарии). Настройки чтения — поповер на той же странице, тёмная тема — одно из трёх значений `readerTheme`, а не отдельный экран | [spec.md](spec.md) |
| **DEC-31** | Подросток выбирает чтение по состоянию («хочу поржать», «когда всё тяжело», «страшное на ночь»), а не по жанру: жанр — язык библиотекаря, а не читателя. При этом главная вела навигацию жанрами (секция `genres_section` — чипы + скроллер, ~330px в середине страницы), а редакционные подборки, уже написанные настроенческим языком, лежали шестой секцией | **Первичный вход в чтение — жинақтар** (`/collections/`): они отвечают на вопрос «зачем читать сейчас», стоят в первом фолде главной и заменяют каталог во втором слоте mobile nav у гостя. Создаёт их **только редакция** — пользовательских подборок на портале нет и не планируется (личное хранение — «Кітапхана»); кнопка «Өз жинағыңды құру» удалена. **Жанр понижается до вывески**: полоса чипов сразу под hero (~44px, ведёт на `/genres/<slug>/`), скроллер произведений активного жанра и параметр `?genre=` на главной удалены. Цветовая система по `Genre.hue` не меняется — жанр остаётся на каждой обложке и в фильтре. **Теги показывают актуальное**: рядом с накопленным `usage_count` появился недельный `weekly_count` («Осы аптада»), страница тега открывается сортировкой `recent`. Новых таксономий не вводится: связь «настроение → произведения» — это `Collection.story_slugs`, которым владеет редакция | [spec.md](spec.md), [ui.md](ui.md) |

| **DEC-32** | FR-STORY-12 сделал лайк поглавным ради «карты качества по главам», но одна кнопка отвечает только на вопрос «зацепило или нет». Для подростковой аудитории это недоговорка: «я плакал» и «мне нравится» — разные высказывания, и подростки говорят первым способом | **Одиночный лайк главы отменяется, вместо него — пять реакций** (`components/reaction_bar.html`, отменяет часть FR-STORY-12): Күлдім · Жыладым · Жүрегім · Шабыт · Таңғалдым. Словарь **закрытый**, пользовательских реакций нет — открытый список означал бы бесконечную модерацию и длинный хвост мёртвых кнопок. Реакции **заменяют** лайк, а не добавляются к нему: хвост главы и так несёт опрос и форму комментария. Каждая реакция подписана словом — эмодзи запрещены, а монохромная SVG 20px без подписи неразличима. В каталоге на карточке остаётся один суммарный счётчик: пять цифр превратили бы сетку в дашборд. `chapter_like.html` удалён | [spec.md](spec.md) |
| **DEC-33** | Блок «Бұл бөлімнен кейін» стоял под каждой главой с тремя захардкоженными вариантами («Батыл қадам» / «Досымен сөйлесу» / «Құпияны іздеу»), одинаковыми для всех произведений, и ни к чему не вёл. Он занимал экран между текстом и переходом к следующей главе | Заменяется **настоящим опросом автора** (FR-STORY-13, `components/chapter_poll.html`). Необязательный, создаётся в редакторе главы: вопрос + до 4 вариантов. Для сериальной прозы это главный механизм возврата — «кого он выберет?» приводит читателя за следующей главой, а у автора появляется обязательство дописать. Результаты скрыты до голоса, иначе первый перевес определяет остальные ответы. Опрос **закрывается публикацией следующей главы** — именно там ответ и приходит, сюжетом. Не квиз: правильного ответа, очков и рейтинга угадавших нет. Текст — UGC, модерация через Django admin (DEC-23) и жалобу (FR-STORY-09) | [spec.md](spec.md) |

| **DEC-34** | FR-STORY-03 требовал sticky scrollspy из трёх пилюль-якорей на странице произведения. В коде его нет и не было с момента перехода на inline-чтение; более того, `test_story.StoryDetailValidSlug.test_no_old_scrollspy_anchors` проверяет, что якорей нет, — а карта реализации ссылалась на этот же класс как на *покрытие* требования. Карта утверждала, что фича проверена тестом, который её отрицает | **FR-STORY-03 аннулируется** (отменяет часть DEC-15 в её применении к Story detail; сам DEC-15 — запрет декоративных табов — остаётся в силе). Переключать на странице нечего: DEC-30 сделал её линейным чтением, навигацию по главам несут панель чтения, оглавление в `<details>` и bottom-sheet (FR-STORY-04). Три якоря сверху дублировали бы их и на мобильном отняли бы строку под шапкой. Номер FR-STORY-03 не переиспользуется | [spec.md](spec.md) |

| **DEC-35** | Длина строки в режиме чтения не была задана — она получалась остатком от отступов. На 375px контейнер `px-4` и карточка главы `p-6` оставляли тексту 295px, около 35 знаков при комфортных 45-75. При этом три «настройки чтения» работали против читателя: ось ширины колонки (620/760px) на телефоне не делала ничего, потому что обе величины больше доступной ширины; крупный кегль сужал меру до ≈31 знака; тёплый и ночной фон добавляли свой `padding: 1rem`, то есть смена фона молча отнимала у строки ещё 32px. Отдельно: токен --font-serif (Georgia) три месяца стоял в `@theme` и в showcase, не применяясь ни на одном экране, а редактор главы обещал автору «Georgia, ұзын жолдар» — автор писал в одной гарнитуре, читатель читал в другой (Q-10) | **Меру задаёт `max-width: 68ch`** на тексте главы: единица привязана к кеглю, поэтому число знаков в строке не меняется при увеличении шрифта. На телефоне карточка главы раскрыта во всю ширину экрана (`-mx-4` + свой `px-4`) — тексту 343px вместо 295. Подложка темы отделена от текста: тинт доходит до края карточки отрицательным полем, ширину строки не трогает. **Ось «ширина колонки» заменена на межстрочный интервал** (`reader-lead-tight` 1.6 / `reader-lead-airy` 1.9) — она работает на любом экране, в отличие от прежней; ключ в `localStorage` — `bp-reader-lead`. Оси разведены по свойствам: размер задаёт только `font-size`, интервал — только `line-height`. **Токен --font-serif удалён** (закрывает Q-10): чтение и редактор набираются `Open Sans` одним кеглем. Прогресс-бар в шапке главы скрыт на мобильном — панель чтения уже несёт тот же счётчик | [ui.md](ui.md), [spec.md](spec.md) |
| **DEC-36** | Каталог отвечал на вопрос «какие свойства у объекта», а не на вопрос читателя. Дефолтом стояла накопленная популярность (`views desc`) — то есть рейтинг просмотров, при том что докблок «культура продукта» определял Wattpad-культуру как «популярность важнее качества» и противопоставлял ей свою; первую страницу навсегда занимали несколько старых хитов. Знаки качества самой платформы — «Редакция таңдауы» (там же — «редкий знак качества») и «Байқауға қатысады» — лежали в `Story.badges`, рендерились неотличимой подписью на карточке и не фильтровались вовсе. Комбинация `format=single + length=short`, которую тот же докблок дословно называл быстрым чтением, стоила двух тапов в двух разных группах панели | **Дефолт каталога — окно в 14 дней**: сортировка `trending` («Қазір танымал») по `Story.recent_views`, `popularity` остаётся отдельным пунктом («Ең көп оқылған»). Накопленный счётчик отвечает на «что читали когда-то», каталог должен отвечать на «что читают сейчас». **DEC-31 не отменяется**: страница тега остаётся на `recent` — там ценна свежесть сама по себе, а не набранные за две недели просмотры. Вводится **ось знака качества** `badge` (`STORY_BADGES`: `editorial` / `contest`) — единственная ось, где качество заявлено отдельно от просмотров. Вводятся **пресеты «Не оқимын?»** (`CATALOG_PRESETS`) — готовые комбинации осей одним тапом, с настоящим счётчиком выборки; пресет с нулём не показывается, активный поглощает чипы своих осей. Пресеты стоят в контенте, а не внутри панели сүзгі: на телефоне панель живёт за модалкой, и главный вход в чтение оказался бы спрятан под кнопкой. **Жинақтар** (DEC-31) добавлены в рейл каталога и в пустое состояние — пустой экран перестаёт быть тупиком. Попутно восстановлено **DEC-23**: `filter_catalog` стартовала с полного `STORIES`, и работа со статусом «Модерацияда» открыто лежала в публичном каталоге; теперь публичны только `Published`/`Completed`/`OnProcess` | [spec.md](spec.md), [architecture.md](architecture.md) |

| **DEC-38** | Ось «Жас» в каталоге фильтровала **точным совпадением** `Story.audience`. Для возрастной отметки это работает наоборот: четырнадцатилетний, выбравший «14+», терял пятнадцать из двадцати одной работы, которые ему полностью доступны, — каталог показывал ему шесть. Подпись повторяла ключ («10+», «14+») и читалась как отметка произведения, а не как вилка читателя, поэтому выбор выглядел как «покажи только взрослое» | Ось становится **накопительной** и переименовывается в **«Жасың»** — она про читателя, а не про работу. Порядок отметок задан `AUDIENCE_ORDER = ("10+", "14+")`, фильтр сравнивает по индексу: выбравшему «14+» доступно и то, что помечено 10+. **Безопасное направление сохраняется и было главным аргументом за смену**: младшая вилка старших отметок по-прежнему не показывает, то есть риск «ребёнок увидел 14+» не меняется ни в одну сторону. Подписи разведены с ключами: ключ остаётся отметкой работы (верхняя граница доступного), подпись называет вилку читателя — «10-13» и «14+». Ключи в URL не менялись, старые ссылки живы. Альтернатива — оставить точное совпадение как «покажи мне что посерьёзнее» — защитима, и если в эксплуатации окажется, что старшие подростки хотят именно её, решение отменяется новым DEC | [spec.md](spec.md) |

| **DEC-37** | `Story.status` держал две несовместимые вещи: путь модерации (Жоба → Модерацияда → Жарияланды) и завершённость сериала (Аяқталды / Жазылып жатыр). В каталоге это давало ось, которая ничего не отбирала: «Жарияланған» стояло у 90% выдачи, потому что в каталоге всё и так прошло модерацию. Данные вдобавок были разложены наоборот — обе читательские метки стояли на одночастевых произведениях, где завершённость бессмысленна, а все десять сериалов были помечены просто «Жарияланған», так что узнать, дописан ли сериал, было нельзя ни по одному. Рядом стояла вторая ось «Формат», отвечавшая на половину того же вопроса | **Оси «Формат» и «Мәртебесі» в каталоге заменяются одной — «Түрі»** (`kind`): `single` (цельный текст) · `done` (аяқталған сериал) · `ongoing` (жазылып жатыр). Три значения покрывают все осмысленные сочетания и отбрасывают невозможное — `single` + «пишется». Значения «любой сериал» нет намеренно: оба места, которые вели на `?format=serial` (ряд главной и пресет), означали «продолжается». **Правило данных:** среди публично видимых работ `single` носит `Published`, а `serial` — `Completed` или `OnProcess`; `Published` у сериала невалиден и закрыт тестом. `status` остаётся полем модели, живёт в кабинете автора и в `status_badge` (BR-10 не меняется), ~~`format` остаётся полем и legacy-параметром~~ — обе оси уходят из панели каталога; параметр `?format=` снят целиком в DEC-49, поле модели остаётся. **Литерал `'Published'` заменён на `PUBLIC_STATUSES`** в рядах главной, в индексе Cmd+K и в проверке «у каждого жанра есть что показать»: по одному литералу оттуда молча пропали бы все сериалы разом, отдавая при этом 200. Попутно `writer_stats` считал `published` тем же литералом и звал `drafts` то, что подписано «Жазылып жатыр», — счётчик включает `Completed`, ключ переименован в `ongoing` | [spec.md](spec.md), [architecture.md](architecture.md) |

| **DEC-39** | Статус `NotPublished` рендерился бейджем `error` — красным, тем же, что «Қабылданбады» (отказ жюри), кнопка удаления и страница 500. Это дефолтный статус нового произведения (BR-10), то есть первое, что видел автор, создав работу, — отметка об ошибке на тексте, который он ещё даже не дописал. Тон был поправлен только наполовину: «Жарияланбаған» → «Жоба» (словарь тона) сменило слово, но не цвет, и красным оставалось помечено нормальное состояние работы. Токен `--color-status-error-*` при этом так и назывался «Жарияланбаған», хотя этого текста в интерфейсе уже не было | **`NotPublished` носит нейтральный бейдж** (`neutral`: slate-100 / slate-700). Черновик не нарушил никаких правил — он просто ещё не опубликован, и серый читается как «не в эфире», что и есть правда. Красный остаётся тому, что действительно означает отказ или необратимое действие: `Қабылданбады`, `delete_confirm_modal`, `error_state`, 500. Роль токена переписана в `ui.md` и в `@theme`. Текст статуса не менялся — только семантика бейджа; словарь тона в `ui.md` остаётся единственным источником строк. **Не отменяет ни одного DEC**: красный цвет черновика никогда не был решением, он был умолчанием | [ui.md](ui.md), [spec.md](spec.md) |

| **DEC-40** | Авторский кабинет описывал имущество, а не отвечал на вопрос, с которым автор в него заходит. Порядок списка был порядком объявления в `STORIES` — единственная опора «что я трогал последним» отсутствовала, поля даты не было вовсе. Работа висела «Модерацияда» без признака, сколько она там, и показывала при этом «0 · 0 · 0» — три нуля вместо ответа на единственный вопрос, который к ней есть. Просмотры показывались накопленные, хотя DEC-36 уже ввёл окно в 14 дней ради ровно этого различия: «1 042 за всё время» не говорит, читают ли автора сейчас. Новые пікір были видны только в другом разделе, черновик ничем не отличался от готовой работы, а из списка нельзя было ни посмотреть работу глазами читателя, ни открыть баптаулар, ни удалить — любое из трёх стоило захода в «Басқару». Попутно: `Author.works` был хранимым литералом и врал у всех шести авторов сразу (`rudazov` — 12 при трёх работах), а число рендерится в шести местах, включая карточку автора на странице произведения | **Кабинет отвечает «что дальше», как каталог после DEC-36 отвечает «что читать».** Вводится **полоса «Назарыңды күтеді»** (FR-WRITE-08): модерация, непрочитанные пікір, черновик без бөлім — три сигнала, которые уже лежали в данных и нигде не сходились; ссылка ставится только когда за сигналом одна работа, полоса не рендерится, когда сигналов нет. Список **сортируется по последней правке** — новое поле `Story.updated_days_ago` (в Ф14 — `updated_at` с auto_now; дельту хранить нельзя, она устаревает за сутки), работы без даты уходят в конец. Строка показывает **`recent_views`, а не `views`**, а у непубличной работы вместо нулевых метрик — состояние процесса («4 күн тексеруде», «әлі бір бөлім жоқ»). Добавлено **меню строки** «Сайтта қарау · Баптаулар · Жою»; видимость первого пункта — по `Story.is_public` (PUBLIC_STATUSES), не по литералу, иначе пункт пропал бы у всех сериалов (DEC-37). Обложка слева на всех ширинах: ниже `sm` карточка разворачивалась в колонку и в экран влезали две работы. **`Author.works` становится производным** и считает только публичные статусы: черновик публично не виден (BR-10), и в публичный счётчик попадать не должен. **Пагинация и группировка черновиков сознательно не вводятся** — у автора в стабе пять работ, и то и другое было бы непроверяемым кодом; порог вводится, когда появятся реальные данные | [spec.md](spec.md), [architecture.md](architecture.md) |

| **DEC-41** | Профиль автора показывал четыре числа и сетку обложек — и не отвечал на единственный вопрос, с которым на него заходят: стоит ли этого автора читать. Признание автора нигде не фиксировалось: победа в конкурсе жила текстом в `Contest.description`, «Редакция таңдауы» оставалась подписью на карточке произведения и до автора не поднималась, а факт «я здесь давно» не хранился вовсе. Прямой запрос был на **рейтинг** — число, по которому авторов можно выстроить в ряд | **Знаки, а не рейтинг.** Вводятся семь знаков (FR-PROF-06): алғашқы жарияланым, сериалды аяқтады, байқауға қатысты, байқауға қабылданды, байқау жеңімпазы, редакция таңдауы, ступени оқылым. **Числового рейтинга автора не будет** — это прямое следствие принципов продукта («не делать лайки главным символом успеха», «главная не должна выглядеть как рейтинг»): авторам 14-18, монетизации нет, авторов мало — публичное число, которое падает, подросток читает как оценку себя. Знак говорит «ты сделал», рейтинг — «ты хуже вон того». **Все знаки выводятся из данных, ни один не хранится**: `Author.works` уже был хранимым литералом и врал у всех шести авторов сразу в шести местах рендера (DEC-40) — знаков семь, и разъезжаться они начали бы в семь раз быстрее. Ступени `READ_TIERS` считаются **накопительно по публичным работам** (это знак автора, у произведения свои `STORY_BADGES`), в публичный ряд идёт **только высшая**: «Мың» + «Он мың» + «Елу мың» рядом — три пилюли об одном. Акцентный `kind` — только у двух редких знаков: «редкий знак качества, не массовый бейдж»; подсветить все семь значит не выделить ни один. `Contest.winners` хранит **слаги произведений**, автор выводится через `Story.author_username` — второй литерал с именем разошёлся бы с первым | [spec.md](spec.md), [architecture.md](architecture.md) |
| **DEC-42** | Обсуждался авторский блог на профиле | **Блога не будет.** Три причины, по которым автор просит блог, закрыты существующим: «когда новая глава» — публикацией главы и уведомлениями, «расскажу о себе» — `bio` и вкладкой «Туралы»; остаётся благодарность читателям, ради которой подсистему не строят. Цена — второй тип контента со своей лентой, модерацией, жалобами и комментариями, при **одном** модераторском потоке (Django admin, DEC-23) и аудитории несовершеннолетних, где каждое свободное публичное поле — поверхность безопасности. Наблюдаемый исход таких стен (профильные «Conversations» у Wattpad) — не авторские заметки, а очередь «когда прода», то есть давление на подростка, который и так боится не дописать. Если потребность подтвердится поведением, дешёвый шаг — одна закреплённая заметка «Автордан хат» (≤280 знаков, без комментариев, без ленты, без архива), то есть расширенный `bio`: поле, а не подсистема | [spec.md](spec.md) |

| **DEC-43** | Награды из DEC-41 рендерились пилюлей `badge.html`: монохромная иконка 12px плюс подпись словом. Семь таких пилюль в ряд читаются таблицей, а не наградами, и ничем не отличаются от статусов и тегов, которые рисуются той же пилюлей. Признание выглядело служебной пометкой | **Награда — предметная иллюстрация на постаменте, а не пилюля.** Отдельный компонент `components/award.html`, спрайт `components/awards/_sprite.html`. Работает **система**, а не набор картинок: общий постамент одинаковой геометрии (он и превращает набор в набор), уникальная эмблема сверху, металл как ступень ценности. Металл заменил акцентный `kind` из BR-ACH-02 — читается без легенды и не требует подписи рядом; реестр металлов закрыт (`ui.md`). Уникальных рисунков **семь, не десять**: ступени оқылым — один рисунок-стела, у которого меняются число на табличке и металл. **Подписи рядом нет** — ряд из семи подписанных пилюль и был проблемой; смысл отдаёт тултип. Но тултип-только-по-ховеру означал бы, что на телефоне смысл недоступен вовсе, поэтому награда — `<button>`, открывающийся по наведению, фокусу **и тапу** (BR-ACH-06), с `aria-label`; нативный `title=` не годится, на тач-устройствах он молчит. Спрайт подключается **только на профиле**, не в `base.html`: иконочный спрайт уже весит ~22 КБ инлайном на каждой странице. Постамент **инлайнится в каждый символ**, а не подключается вложенным `<use>` на группу в `<defs>` — такая вложенность рендерится не во всех движках, и постамент молча пропадал, оставляя эмблему висеть в воздухе | [ui.md](ui.md), [spec.md](spec.md), [architecture.md](architecture.md) |
| **DEC-44** | `/me/?tab=works` рендерил `my_stories_of` строками `my_story_row` — то есть ровно список из `/my-stories/` минус полоса внимания. Две страницы с одинаковым содержимым, и ни одна не отвечала, зачем она; счётчик сегмента при этом считался по двум разным правилам, из-за чего «Шығармалар 5» у владельца открывало список из трёх у постороннего | **Профиль — публичный вид на автора, кабинет — рабочее место.** Вкладка «Шығармалар» своего профиля показывает `public_stories_of` теми же карточками, что видит читатель. Черновики, статусы и «когда трогали» остаются в `/my-stories/`; счётчик сегмента считается по одному правилу для обоих (`_prof_items`). **Информация о непубличном не теряется**: разбивка по статусам стоит во вкладке «Статистика» под пометкой «Тек саған көрінеді» (FR-PROF-08), а над списком работ автор видит строку «Тағы N жұмыс оқырманға көрінбейді» со ссылкой в кабинет — молча спрятать работы нельзя. Сегмент не убран совсем (вариант «оставить только ссылку в кабинет»): у чужого профиля он есть, и его отсутствие у своего пришлось бы объяснять | [spec.md](spec.md) |

| **DEC-45** | У конкурса было два статуса — `active` и `finished`, — заведённых руками вместе с `days_left`, `year` и числом заявок. Между дедлайном и объявлением итогов конкурс либо показывал «Белсенді, 0 күн қалды», либо резко становился «Аяқталды» без единого победителя; фазы «объявлен, но приём ещё не открыт» не существовало вовсе. Хранимые производные при этом врали: «87 өтінім» стояло при одной настоящей заявке, а этап «Өтінім қабылдау» конкурса 2024 года был помечен `active` в 2026-м | **Фаза выводится из трёх дат, а не хранится.** `opens_on` · `closes_on` · `results_on` — единственное, что заводит админ; из них считаются `phase` (`upcoming` · `accepting` · `judging` · `finished`), отсчёт дней, год проведения и состояние каждого этапа таймлайна. Число заявок считается по самим заявкам. Четвёртая фаза (`judging`) заведена ради промежутка «приём закрыт, победители не названы», пятой не нужно. **«Можно подать» и «конкурс не завершён» — разные вопросы**, и слово «активный» их смешивало: кнопку «Қатысу», баннер главной и доступ к форме подачи решает `is_accepting`, бейдж работы «Байқауға қатысады» — `not is_finished`. Даты идущих конкурсов в стаб-данных заданы **относительно сегодня**: абсолютные литералы протухают молча, и именно так «Алтын қалам — 2024» остался активным в 2026-м. Год ушёл из слага того конкурса — он теперь выводится из даты итогов. Отменяет двухстатусную часть BR-40 | [spec.md](spec.md), [architecture.md](architecture.md), [ui.md](ui.md) |
| **DEC-46** | Награда за конкурс была одна на все конкурсы всех лет — системный знак «Байқау жеңімпазы» из закрытого кортежа `AWARDS`. Он не отличал первое место от приза зрительских симпатий, не называл ни конкурс, ни год, и завести свой знак под свой конкурс админ не мог: реестр — код, иллюстрация — рукописный `<symbol>` в спрайте. `Contest.winners` был плоским кортежем слагов, места в нём не было | **У каждого конкурса свой произвольный набор номинаций, эмблему грузит админ файлом.** `ContestAward` (слаг · название · эмблема · описание) живёт внутри конкурса; общего реестра номинаций нет — он и есть то, чем один конкурс отличается от другого. Победа фиксируется `AwardGrant` (конкурс + номинация + работа) и **хранится**: «1-орын» из данных не вычисляется, это акт жюри. Хранится именно присуждение, а не список наград у автора — колонки `Author.badges` как не было, так и нет (BR-ACH-01), и ряд в профиле остаётся запросом. `Contest.winners` стал производным от присуждений. **Эмблема — растр от админа, рама — от платформы**: медальон, кольцо, тень рисует компонент, иначе через десять конкурсов ряд превратился бы в коллекцию чужих JPEG. Силуэт (диск против статуэтки на постаменте) различает два класса знаков без подписи, поэтому золото остаётся редким знаком трёх системных наград (BR-ACH-02) — у конкурсной награды металла нет вовсе. SVG к загрузке не принимается: файл из `/media/` открывается в origin сайта и может нести скрипт. Номинации показываются **до** итогов — «вот что получит победитель» отвечает на «зачем участвовать» лучше, чем сумма в тенге. Системный «Байқау жеңімпазы» снят: держать оба значило бы дважды сказать одно и то же, причём менее точным способом. Отклонено: закрытый набор форм наград вместо загрузки (админ не смог бы отразить номинацию своего конкурса) | [ui.md](ui.md), [spec.md](spec.md), [architecture.md](architecture.md) |
| **DEC-47** | Продукт объявлял, для кого он. Подсказка поля «Жасы» на регистрации и в редактировании профиля говорила «Байқауға қатысу үшін 14-18 жас керек», чек-лист подачи держал пункт «Жас 14-18», а четыре конкурса из пяти повторяли ту же вилку в условиях. Между тем платформой пользуются не только школьники, но и студенты колледжей и вузов, и заявление о возрасте отсекало часть аудитории раньше, чем она успевала что-либо увидеть | **Аудитория — внутренняя информация; продукт её не называет.** Возрастного ценза у платформы нет: вилку ставит **конкурс** (BR-48), и называет её страница этого конкурса — единственное место, где она законна, потому что автору нужно знать, подавать ли ему. Поле возраста на регистрации остаётся (конкурсам оно нужно), но диапазона не называет и объясняет, зачем спрашивает. Чекбокс подтверждения на подаче генерится из требования конкурса; у конкурса без ценза его нет. Правило держит `test_lint.PlatformDoesNotNameItsAudience`: вилка ищется и в отрендеренном HTML всех публичных маршрутов кроме конкурсных, и в исходниках шаблонов — иначе строка вернётся с первым же новым текстом, как уцелело «конкурс» в пустом состоянии списка. Отклонено: заводить статус участника (школьник / студент колледжа / студент вуза) отдельным полем — требования пока меняются только по возрасту; понадобится статус, он добавится полем на `Contest`, ничего не ломая | [architecture.md](architecture.md), [spec.md](spec.md), [ui.md](ui.md) |
| **DEC-48** | Статистика автора жила в правом рейле WRITE-страниц (партиал `writer.html`) и в полосе под шапкой списка. Четыре плитки — Шығарма · Оқылым · Ұнатулар · Жазылушы — слово в слово повторяли `partials/profile/_stats.html`, где они к тому же кликабельны, а разбивка по статусам повторяла вкладку «Статистика» профиля, роняя при этом черновик: «Жарияланды 2 + Модерацияда 1 + Жазылып жатыр 1» стояло под «Шығарма 5» вопреки BR-ACH-07. Хуже всего рейл вёл себя на страницах **одного произведения**: в шапке `manage_story` стояло «1 042 оқылым» этой работы, а в рейле рядом — «Оқылым 2 117» по всему портфелю, и нигде не было сказано, что это разные вопросы. Кнопку «Жаңа шығарма» рейл дублировал из шапки того же списка | **Агрегаты автора живут в одном месте — профиле.** Рейл писателя удалён со всех пяти WRITE-страниц вместе с файлом; `has_right_rail` там больше не выставляется, и колонка контента центрируется по DEC-25. Разделение, уже записанное в FR-PROF-08, теперь соблюдается в обе стороны: **профиль отвечает на «как идёт», кабинет — на «что делать»**. Ничего не потеряно: суммы и разбивка стоят в профиле (и там сходятся с целым), «сколько висит на модерации» показывает полоса «Назарыңды күтеді» со ссылкой на конкретную работу (FR-WRITE-08), а кнопка создания и так в шапке списка. Единственный вход в агрегаты — тихая строка под списком, ведущая в `/me/?tab=stats`. Вместе с полосой ушёл компонент `stat_strip.html`: он существовал ровно ради мобильной копии этого рейла и других вызовов не имел. Отклонено: **чинить подписью** — добавить рейлу заголовок «Барлық шығармаларың бойынша». Двусмысленность ушла бы, дублирование профиля осталось, а блок про все работы на странице одной работы всё равно не к месту. Отложено: рейл `manage_story` **про эту работу** (конкурс, разбивка реакций по главам) — осмысленно, но это новый виджет, а не устранение дубля | [ui.md](ui.md), [spec.md](spec.md) |

## Закрытые UX-аудиты → результат

| Аудит | Главная находка | Что сделано в ТЗ |
|-------|-----------------|------------------|
| **Главная** | Три одинаковых ряда, нет редакторского голоса | Редакционная витрина: hero-конкурс, книга недели, коллекции, новые авторы, состояния гость/возвращающийся, цветные жанры (FR-HOME-*) |
| **Конкурсы** | Страницы конкурсов не существует, 5 разрозненных упоминаний | Полный модуль CONT: список, детальная, подача с чек-листом, мои заявки (FR-CONT-*, BR-22..43) |
| **Story detail** | Декоративные табы, всё видно сразу | Sticky scrollspy, рекомендации вынесены вниз (FR-STORY-02/03, DEC-15) |
| **Right rail** | Соцсети как костыль; школа упомянута 6 раз без страницы | Рейл = продуктовые блоки; соцсети в footer. Школа в MVP — **блок внешних ссылок** (модуль LINKS), не страница (DEC-02/DEC-22) |

## Реестр решений (продолжение)

| # | Что было (проблема / несогласованность) | ✅ Принятое решение | Где описано |
|---|------------------------------------------|---------------------|-------------|
| **DEC-49** | DEC-37 убрал оси «Формат» и «Мәртебесі» из панели каталога, но оставил `?format=` и `?status=` параметрами — «на них ведут ссылки, которые уже могли уйти наружу». Наружу они не уходили: портал не публиковался, а единственная внутренняя ссылка на `?format=single&length=short` (ряд «Қысқа оқылатын әңгімелер» на главной) вела в состояние, у которого с DEC-36 уже есть свой пресет «Бір отырыста». Параметр при этом тянулся через восемь мест: `_catalog_controls`, `_catalog_href`, `_catalog_links`, `_catalog_presets`, словарь состояния, контекст шаблона, скрытое поле формы и обе функции слоя запросов — и требовал внимания при каждой правке каталога | **Ось `?format=` снята целиком, вместе с `CATALOG_FORMAT_FILTERS`.** Ссылка с главной переведена на `?kind=single&length=short` — та же выборка, но живой осью, и целевая страница подсвечивает соответствующий пресет. Редиректа со старого параметра нет намеренно: `format=single` в `kind` переводится, а `format=serial` — нет (значения «любой сериал» у `kind` не существует по DEC-37), и половина редиректа хуже его отсутствия. Неизвестный параметр страницу не ломает — он просто ничего не значит. **Ось `?status=` пока остаётся:** она в том же положении, но у неё нет ни одной ссылки, которую пришлось бы переводить, и снимать её отдельным движением дешевле, чем вместе. Отменяет оговорку DEC-37 про `format` как legacy-параметр; правило данных BR-10a и само поле `Story.format` не меняются | [spec.md](spec.md), [architecture.md](architecture.md) |
| **DEC-50** ⛔ | Работы конкурсов нельзя было прочитать списком: `_winners.html` показывал только победителей, а обзор нескольких конкурсов существовал лишь как одиночный `contest_banner` — баннер ближайшего дедлайна. Напрашивалось решение обернуть работы конкурса в отдельную сущность `Collection` (по образцу DEC-31), но `Collection` — ручная редакционная курация без источника, а состав конкурса уже есть в `Submission`/`AwardGrant`; вторая сущность поверх тех же данных означала бы третий способ узнать одно и то же | **Байқаулар не оборачиваются в `Collection`.** Вместо этого: (1) главная получает секцию **«Байқаулар»** (FR-HOME-14, `data.home_contests()`) — карточки нескольких конкурсов, ведущие прямо на `contest_detail`, отдельно от жинақтар; (2) страница конкурса, сразу после «Сипаттамасы», получает **список участников** (FR-CONT-16, `data.contest_participants(contest)`) — все допущенные работы, не только победители. Видимость — по уже действующему BR-74a (`PUBLIC_CONTEST_RESULTS`): публично видны `accepted`-заявки, победа читается через `AwardGrant`, `reviewing`/`rejected` не показываются. **Лонг-лист/шорт-лист не реализованы** (⛔): `Submission.status` — питоновский `CharField(choices=...)`, а не БД-enum, и `SUBMISSION_STATUSES`/`CONTEST_RESULT_LABELS` — обычные константы; добавить `longlist`/`shortlist` в будущем — значит расширить эти константы и admin-choices, без миграции схемы. Список участников уже рендерится по размеченному `result`, поэтому такое расширение не требует переписывать шаблон | [spec.md](spec.md), [architecture.md](architecture.md) |
| **DEC-51** | `Story.chapters` — «бөлім саны» — хранилась колонкой и объявлялась автором при создании работы. Обновлять её было нечему: `save_chapter` поле не трогал, и работа с пятью написанными главами показывала на карточке «0 бөлім». Держалась колонка ради четырёх сериалов демо-корпуса, у которых текст не написан вовсе (`KNOWN_TEXTLESS`): вычисление из записей обнулило бы им «17 бөлім». То есть поле существовало, чтобы портал мог обещать части, которых никто не писал | **Колонка удалена (миграция 0016).** Число частей выводится: аннотация `chapter_count` (`queries/catalog.chapter_count_subquery`) в выдаче, `chapter_set.count()` у одиночного объекта — тот же контракт, что у `has_chapters` и `total_chars`. Вместе с колонкой ушла и оценка объёма по заявленным частям (`chapters * 1800` в `effective_chars` и `Story.total_chars`): оценивать ненаписанное больше нечем, и работа без текста честно показывает нижнюю границу времени чтения. Четыре сериала корпуса показывают «0 бөлім» — это их настоящее состояние, а наполнение текстом остаётся отдельной задачей. Отменяет оговорку DEC-28 про `chapters == 1`: формат по-прежнему выбирает автор, но сравнивать теперь не с колонкой, а с производным числом | [architecture.md](architecture.md) |
| **DEC-52** | Где читатель остановился, хранилось в двух колонках: `ReadingProgress.current_chapter` и `LibraryEntry.progress_chapter`. Один и тот же факт, два источника — и в демо-корпусе они уже разошлись: `kronchessii` лежал на полке «оқу үстінде» со второй главой, а записи о прогрессе у него не было вовсе. Пока прогресс не писался ниоткуда, кроме сида, расхождение было незаметно; шаг 1.6 плана оптимизации сделал бы его живым | **`LibraryEntry.progress_chapter` удалена (миграция 0017).** Закладку держит `ReadingProgress`, полка получает её аннотацией `progress_chapter` (`queries/library.progress_chapter_subquery`); без записи — первая глава, то есть «лежит, но не открыта». Закладка теперь **пишется при открытии главы** (`record_reading_progress`): `current_chapter`, `last_read_on` и `minutes_left` — сумма глав после текущей. Внутри главы позиции нет (читалка её не сообщает), и прикидывать долю значило бы выдумывать число; `quote` по той же причине не сочиняется, а только сохраняется, если уже есть. Сид заводит закладку у каждой работы на полке «оқу үстінде» и только у них — в корпусе тоже один источник. Тот же ход, что DEC-51 | [architecture.md](architecture.md), [spec.md](spec.md) |
| **DEC-53** | `Tag.usage_count` и `Tag.weekly_count` хранились колонками. Первая была вычислима и без этого, вторая — нет: когда тег появился на работе, нигде не записывалось, и число лежало литералом, который не менялся никогда. А DEC-31 держится ровно на расхождении этих двух чисел: «Осы аптада» имеет смысл, только пока она не копия «Танымал тегтер» — с замершим счётчиком она вырождалась в неё. В корпусе числа вдобавок ничему не соответствовали: тег обещал 42 использования при трёх связках | **Обе колонки удалены (миграция 0018).** Счёт идёт по `StoryTag` — той самой таблице, ради даты в которой связка и перестала быть голым M2M (Ф15): аннотации `usage` / `weekly` в выдаче (`queries/tags.with_counts`), запрос у одиночного объекта. Считаются **только публичные работы**, как у жанров: по счётчику читатель не должен догадываться, что у кого-то есть черновик с этим тегом. Индексы под сортировку по этим колонкам ушли вместе с ними — порядок задаёт агрегат. Сид датирует связки последней правкой работы, иначе `auto_now_add` проставил бы всем момент запуска и обе витрины совпали бы. Закрывает долг DEC-31. Третий случай того же хода — после DEC-51 и DEC-52 | [spec.md](spec.md), [architecture.md](architecture.md) |
| **DEC-54** | Кнопка «Сақталды» не снимала работу с полки ни на одном произведении, у которого есть главы. BR-61 снятие прямо разрешает, но полку двигал сам факт открытия страницы: снятие отвечало редиректом на страницу произведения, та по BR-61 клала работу обратно на «оқу үстінде», и читатель видел «Сақталды» снова. Тем же ходом взгляд на страницу перебивал ручное «сақталған» на `reading`. Невидимо это было потому, что единственной тестовой фикстурой «работы не в библиотеке» была работа **без глав** — у неё автополке нечего было двигать | **Полку двигает продвижение, а не показ.** `move_to_shelf` принимает `advanced` — глава, отличная от той, на которой стоит закладка (а при первом заходе — то, что это не первый голый заход на гл.1, `is_first_look` в DEC-59). Открытие, показывающее ровно то место, где читатель и был, не создаёт и не переводит запись. Неравенство, а не «дальше»: повторное чтение дочитанного начинается с первой главы и обязано вернуть работу на `reading` (BR-61). Закладка (`ReadingProgress`) пишется как раньше, на каждом открытии: она отвечает на «где я остановился», а не на «что у меня на полке». Бюджет страницы вошедшего 37 → 33: первое открытие полку больше не заводит | [spec.md](spec.md) |
| **DEC-55** | Ось «Қазір танымал» (DEC-36) обещает просмотры за две недели, но убывать им было не от чего: `Story.recent_views` и `Story.views` росли на каждом прочтении вместе и никогда не падали. Со временем окно сходится с накопленным счётом, и главная задаёт два разных вопроса — «что читают сейчас» и «что прочли больше всего» — с одним и тем же ответом. Вариант «поколоночная убыль по расписанию» (`recent_views` × коэффициент раз в сутки) отвергнут: это уже не окно, а затухание, и DEC-36 пришлось бы переписать | **Заведён журнал `StoryView`** (`story`, `viewer`, `created_at`; миграция 0019) и команда `recount_views`. Журнал держит **только окно**: команда пересчитывает по нему `recent_views` и тут же удаляет строки старше границы, поэтому таблица растёт с трафиком двух недель, а не с трафиком за всё время — возражение против журнала снято его же вычисткой. Колонка остаётся под `ORDER BY` каталога и **пересчитывается по строкам**, а не сдвигается — тот же приём, что у `Story.likes` и `User.followers`. Накопленный `views` журналом не считается: за пределами окна строк нет по определению. Окно — `domain.story.RECENT_VIEWS_DAYS`: его спрашивают запрос, команда и сид. `viewer` пуст у гостя — читают и без входа. Сид заводит журнал сам, а числа корпуса стали количеством строк: прежние тысячи были декорацией (в интерфейсе окно видно только в кабинете автора) и стоили бы тридцати тысяч строк на каждый прогон суиты. Деплою добавлена суточная задача | [spec.md](spec.md), [architecture.md](architecture.md) |
| **DEC-56** | Девятнадцать миграций Ф14-Ф15 держали историю ходов в схеме: `Story.chapters`, `Tag.usage_count`/`weekly_count` и `LibraryEntry.progress_chapter` заводились с индексами, чтобы позже быть удалёнными (DEC-51, DEC-52, DEC-53). Новая установка проходила девятнадцать шагов ради семнадцати таблиц. `squashmigrations` эту работу не убирает: две миграции с `RunPython` (справочник жанров и перенос тегов на `StoryTag`) стоят барьером для оптимизатора, и схлопнутый файл на 714 строк продолжал бы строить и сносить те же колонки | **Схема пересобрана начальной миграцией** (`0001_initial`, 640 строк) плюс `0002_reference_data` — тот же справочник жанров и блок-лист тегов, что и раньше, без изменений. Сделано, пока база не в проде: `replaces` не заводился, старые файлы удалены. Записи `0001_initial` и `0002_reference_data` в `django_migrations` совпадают по именам со старыми, поэтому существующая база разработчика ничего не доделывает — `migrate --plan` пуст. `TrigramExtension` переехал в `0001` перед триграммными индексами вместе со своим объяснением. **Номера миграций в DEC-51/52/53 указывают на историю, а не на файлы**: сами решения в силе, а колонок, которые они убрали, в схеме больше не появляется вовсе | [architecture.md](architecture.md) |
| **DEC-57** | Ось каталога «Жаңа есімдер» и ряд «Жаңа авторлар» на главной отбирали по числу подписчиков (`NEW_AUTHOR_FOLLOWERS = 150`). Порог был условным с самого начала и перестал работать вовсе, когда шаг 1.8 заменил декоративные числа корпуса настоящими: у всех авторов стало 0-3 подписчика, порог не отсекал никого, и чип честно показывал «21 из 21» — ось существовала и ничего не сообщала. Обратная ошибка не менее вредна: мало подписчиков бывает и у того, кто пишет второй год, и ось звала бы читать его как новичка | **Новизна — возраст аккаунта: `NEW_AUTHOR_DAYS = 365`.** Ось фильтрует по `User.date_joined` внутри окна, граница считается от сегодня и не хранится — колонка с ней устаревала бы каждые сутки. Ряд на главной берёт порядок, а не окно (четверо пришедших последними): раздел обязан быть непустым, и в тихий месяц «последние четверо» честнее пустого места. Год выбран как срок, за который автор успевает выложить первое и понять, продолжает ли он. В корпусе `joined_year` заменён на `joined_days_ago`: 1 января округляло одиннадцать месяцев до нуля в одну сторону и до года в другую, а сама дата теперь относительная, как у работ (шаг 8.1). Двое авторов из шести попадают в окно, ось отбирает 7 работ из 21 — что она различает, проверяется тестом. Закрывает PLAN §6, вопрос 3a | [spec.md](spec.md) |
| **DEC-58** | DEC-32 закрыл словарь реакций пятью словами с монохромной SVG-иконкой в рамке ровно потому, что «эмодзи запрещены, а иконка без подписи неразличима». На практике карточка 88×72px с рамкой, иконкой, словом и числом на 375px не помещалась в один ряд и переносилась 3 + 2 — редкий случай, где собственное правило усложняло короткое, частое действие (реакция ставится под каждой главой) продукту, чья аудитория — подростки, которым эмодзи как раз родной язык отклика | **Читательская кнопка реакции — эмодзи без рамки, без подписи словом и без поясняющих строк над/под рядом**, точечное исключение из общего запрета эмодзи (`docs/ui.md`): Күлдім·😂, Жыладым·😭, Жүрегім·🥰, Шабыт·🔥, Таңғалдым·🤯. Счётчик под эмодзи остаётся. Пять квадратных кнопок 56px без рамки умещаются в один ряд начиная с 320px. Доступное имя не потеряно — оно в `aria-label` кнопки вместе со счётом («Күлдім, 12»), а не пропало вместе с видимым текстом; своя реакция получает лёгкую заливку фона вместо рамки. `Reaction` получает поле `emoji` рядом с `icon`; `icon` остаётся и используется в разборе по главам для автора (`manage_story`) — это другой контекст (не частое действие читателя, а разовый просмотр аналитики), там иконка, слово и рамка по-прежнему в силе. Отменяет часть DEC-32 (обязательную подпись словом и рамку у читательской кнопки), сам закрытый словарь из пяти реакций и правило «эмодзи запрещены везде, кроме UGC» вне этого компонента — не отменяются | [ui.md](ui.md), [spec.md](spec.md) |
| **DEC-59** | Первая глава сериала при «голом» заходе (без `?chapter=`, без прежней закладки) рендерилась тизером: `max-h-[60vh] overflow-hidden` + градиент-затухание + кнопка «Жалғастыру», разворачивающая текст по клику (`x-data="{ expanded: false }"`). На читалке с настраиваемым кеглем 60vh — величина, которая ничего не говорит о содержимом: короткая глава пряталась почти целиком, длинная обрывалась на середине абзаца, а разворот терялся при следующей загрузке главы (состояние жило в Alpine, не в `localStorage`) | **Обрезка текста убрана.** Глава — и первая, и любая другая — рендерится полностью с первого показа; блок `x-data="{ expanded: false }"`, градиент и кнопка «Жалғастыру» удалены из `partials/story/chapter.html`. Сигнал «это первый голый заход на гл.1, а не осознанное чтение» **не исчез** — он был нужен не для обрезки, а для двух вещей, которые обрезки не касались: BR-61 (полку не двигает предварительный просмотр, только подтверждённое чтение) и дедуп счётчика прогресса «N / M» с панелью чтения (DEC-54, FR-STORY-*). Эта часть логики осталась в `story_detail`, только переименована: `is_teaser` → `is_first_look` — имя, которое не врёт про несуществующий больше тизер | [spec.md](spec.md) |
| **DEC-60** | `Story.likes` заводился в `_corpus.py` отдельным литералом рядом с `views`/`comments`, а реакции по главам — отдельными кортежами `reactions=(...)` в `CHAPTERS_BY_STORY`. Два независимых источника одного числа: у `dalney-berega` их когда-то сверили руками, а у 19 из 23 работ — нет. Хуже всего у `arhimag`: литерал `4821` — явное наследие одиночного лайка до DEC-32, главы заведены вовсе без `reactions=(...)`, и после первого же настоящего голоса шапка показывала «4822», а сумма пяти эмодзи под главой — «1». Расхождение не ловил `test_seed.SeededChapterReactionsAddUp.test_story_total_equals_the_sum_of_its_chapters`: тест явно пропускает работы без затравленных реакций (`if not any(c.reactions.exists()...): continue`) — то есть проверял только те 4 работы, где число и так набито вручную правильно | **Поле `likes` убрано из `Story` в `_corpus.py`.** Источник истины один — `Chapter.reactions`: `seed_demo._seed_chapters` считает сумму по только что засеянным `ChapterReaction` и пишет её в `Story.likes` тем же проходом, каким заводит сами реакции — вторая независимая цифра завестись не может. Инкрементальное сопровождение `Story.likes` живыми голосами (`toggle_chapter_reaction`, `F()`) не менялось — оно и раньше было верным, ломалась только стартовая точка. Побочный эффект: у 19 работ, которым реакции никогда не сочинялись, `Story.likes` стал честным нулём — `test_write.TheCabinetAnswersWhatToDoNext.test_metrics_are_exact_spoken_and_never_zero_filler` этого не пережил (кабинет обязан показывать точное число, а не нулевой филлер), поэтому трём демо-работам Айданы (`aidana-tan`, `aidana-koshe`, `aidana-kysh`) заведены небольшие, но настоящие `reactions=(...)`, а не подогнанное число. Правило данных: у корпуса теперь ровно один источник каждого агрегата, вторым он обрастать не должен | [architecture.md](architecture.md) |
| **DEC-61** | DEC-60 научил `seed_demo` пересчитывать `Story.likes` из `ChapterReaction` при каждом запуске — но `_seed_chapters` заодно продолжала **безусловно** стирать и пересобирать `ChapterReaction` из корпуса (`chapter.reactions.exclude(kind__in=kinds).delete()` + `update_or_create`), не глядя, есть ли у главы настоящие голоса. У `arhimag` ровно так и вышло: живой голос (`ChapterReactionVote`) уже стоял на главе, повторный `seed_demo` обнулил её `ChapterReaction` до пустого корпуса (`reactions=()` у `arhimag` никогда не заводились), а следующий клик — смена вида реакции того же голосующего — по BR-14a не трогает `Story.likes` вообще. Итог: под главой честно «1 реакция», а в шапке — «0»: не рассинхрон вычисления, а сид, среди бела дня стерший то, что человек уже сделал | **Глава с настоящим голосом (`ChapterReactionVote.objects.filter(chapter=...).exists()`) реакции больше не трогает вовсе** — ни `ChapterReaction`, ни свой вклад в `Story.likes`, который теперь берётся с самой главы (`chapter.likes`), а не пересчитывается из корпуса заново. Декоративная цифра `reactions=(...)` кладётся только на пустое место — туда, где ни один человек ещё не голосовал; как только голос появился, корпус для этой главы замолкает навсегда, и дальше ею распоряжается только `toggle_chapter_reaction`. Это тот же принцип, что уже действует для `LibraryEntry` (ручная полка не перебивается взглядом на страницу, DEC-54) и для черновика (`NotPublished` не красится в error, DEC-39) — сид и рантайм каждый раз заново задают вопрос, у кого приоритет, когда данные разошлись, и ответ один: у настоящего действия читателя, не у фикстуры | [architecture.md](architecture.md) |
| **DEC-62** | Проверка по мотивам DEC-61 на соседнем счётчике: `Story.comments` — та же болезнь, что была у реакций, только опаснее. `_seed_stories` брала декоративный литерал корпуса, а `_seed_comments` **безусловно удаляла все комментарии работы** (`story.comment_set.all().delete()`) и пересобирала только пригоршню демонстрационных из `COMMENTS_BY_STORY`. Живой `add_comment()` исправно прибавлял к `Story.comments` через `F()+1` — но следующий же `seed_demo` стирал не только счётчик, а сам текст читателя целиком, без следа | **`_seed_comments` апсертит демо-комментарии по естественному ключу** (story, author, chapter_number, parent, text) через `update_or_create` вместо delete-и-пересоздать — то, чего нет в этом ключе (настоящий текст читателя), никто не трогает. `Story.comments` считается здесь же: декоративная база корпуса (`stub.comments`) плюс число строк сверх апсернутого набора — тот же приём, что у `Story.likes` в DEC-60/61, применённый ко второй колонке, где корпус тоже врёт бо́льшим числом, чем реально смоделировано. Проверка `Story.views`/`recent_views` на тот же класс бага в этом заходе ошибочно закрыта словами docstring `_seed_views` («накопленный `views` остаётся литералом») — это описание того, что колонка **на тот момент** так себя вела, принятое за оправдание, почему это правильно. Это не одно и то же: сброс декоративного литерала при каждом пересиде уничтожает и `StoryView`, и накопленный счётчик так же безусловно, как раньше уничтожались реакции и комментарии, а `recent_views` вдобавок — ось сортировки каталога по умолчанию (DEC-36), то есть цена ошибки выше, чем у соседних двух. Разбирается отдельно — DEC-63 | [architecture.md](architecture.md) |
| **DEC-63** | То же, что DEC-61/62, третий счётчик подряд, и здесь цена выше: `_seed_views` вызывала `StoryView.objects.all().delete()` **безусловно и глобально** — не по одной работе, а весь журнал прочтений сразу, каждый прогон, — и `_seed_stories` присваивала `Story.views` decorative-литерал корпуса тем же безусловным `update_or_create`. Живой `record_story_view` исправно вёл счёт (`F('views') + 1`, реальная строка в `StoryView`), но следующий пересид стирал и то, и другое без остатка. Хуже, чем у `likes`/`comments`: `recent_views` — не просто цифра в шапке, это **ось сортировки каталога по умолчанию** (DEC-36, «Қазір танымал»); сброс к декоративному состоянию тем же движением подменяет всю выдачу каталога, а не одну карточку. Оправдание было готово заранее — docstring `_seed_views` прямо заявлял «накопленный `views` остаётся литералом», и это описание было принято за причину, а не опознано как то же нарушение, что уже дважды нашлось рядом | **`_seed_views` больше не удаляет журнал.** `Story.views` поднимается до `stub.views` только если сейчас меньше (`update(views=stub.views)` с условием `views__lt=`) — никогда не опускается. Окно `recent_views` довешивается: недостающие decorative-строки `StoryView` добавляются до `stub.recent_views`, лишнее (то есть реальный трафик сверху) не трогается и не удаляется. Пересчёт `recount_recent_views()` (DEC-55) как считал по журналу, так и считает — ему всё равно, откуда строки. Тот же принцип в третий раз: сид поднимает пол, никогда не срезает то, что уже начитано живым читателем | [architecture.md](architecture.md) |
| **DEC-64** | Разбор без оглядки на документацию нашёл тот же класс бага ещё в двух местах, отдельно от DEC-61/62/63. **`PollOption`/`PollVote`**: `_seed_polls` делала `poll.option_set.all().delete()` + `bulk_create` на каждый прогон; `PollVote.option` объявлен с `on_delete=CASCADE`. Пересид не просто сбивал счётчик `votes` (`F()+1` в `cast_poll_vote`) — он **каскадом удалял саму запись голоса**, а `cast_poll_vote` не пускает второй голос именно через её существование («один голос на опрос, не меняется»). То есть после пересида читатель мог проголосовать в уже пройденном опросе второй раз — не рассинхрон цифры, а нарушение BR-POLL. **`Notification`**: `user.notifications.all().delete()` в `_seed_notifications` стирала все уведомления пользователей из корпусной ленты, включая настоящие — единственный живой источник сейчас — `Story.apply_moderation` (DEC-23); остальные типы FR-NOTIF-03 (комментарий, реакция, новая глава, подписчик) сегодня не генерируются вовсе, лента и так почти целиком декоративна, поэтому масштаб риска у этого случая уже — только решения модерации | **Обе апсертятся по естественному ключу вместо delete-и-пересоздать**, тот же приём, что у DEC-61/62. `PollOption` — по (poll, slug): существующая строка и её `pk` не трогаются, `PollVote.option_id` не рвётся, счётчик `votes` обновляется на месте. Опрос с хотя бы одним настоящим голосом (`PollVote.objects.filter(poll=poll).exists()`) сид больше не трогает вовсе — тот же уровень защиты, что у главы с реакцией (DEC-61). `Notification` — по (user, kind, actor, story, contest, text): то, чего нет в этом ключе (настоящее уведомление), никто не трогает. Проверено на живых данных: настоящий голос и настоящее уведомление о модерации переживают повторный `seed_demo`, повторное голосование остаётся заблокированным | [architecture.md](architecture.md) |
| **DEC-65** | DEC-27 развёл `/search/?q=...`, `/genres/<slug>/` и `/tag/<slug>/` по разным canonical-URL ради SEO и шеринга — но снаружи портал ещё не запущен (NFR-25 ⛔), и в реализации search-режим несколько шагов подряд расходился с browse: получил собственную (позже урезанную) шапку, отдельное idle-состояние «Не іздейміз?» при пустом запросе, и вовсе лишился панели фильтров/пресетов — то, что для страницы каталога оставалось нормой. Три режима одного движка (DEC-27) фактически стали двумя разными продуктами поверх одной выдачи, и разница всё время требовала отдельного внимания при любой правке каталога | **Поиск перестаёт быть отдельным режимом.** `?q=` — обычный параметр `/catalog/` (и `/genres/<slug>/`, и `/tag/<slug>/`, как и раньше): та же панель фильтров, те же пресеты «Не оқимын?», тот же рейл. `mode='search'` и `search_results`-рендер удалены; `_render_catalog` их не различает. Idle-состояние снято — пустой `?q=` на `/catalog/` теперь просто показывает каталог, как и без параметра. `/search/` остаётся рабочим адресом: view стал тонким редиректом на `/catalog/` с тем же querystring — старые ссылки и закладки не 404. Шапка, Cmd+K popup и мобильный FAB ведут на `/catalog/` напрямую. Заголовок каталога (`_hero_catalog.html`) стал единственным hero каталожных страниц вне жанра/тега — с запросом называет его («‹query› бойынша іздеу»), без — нейтральный «Каталог»; `_hero_search.html` удалён. Отменяет часть DEC-27 (отдельная entry-страница поиска ради SEO) — до публичного запуска цена держать её отдельной от каталога перевесила пользу | [architecture.md](architecture.md), [spec.md](spec.md) |
| **DEC-66** | Страница произведения не смотрела на статус вообще: `story_by_slug` был `all_stories().filter(slug=slug).first()`, и черновик вместе с работой на модерации отдавался целиком **кому угодно, включая гостя без входа** — название, аннотация, полный текст глав. Проверено на живом сервере: анонимный запрос черновика возвращает 200 со всем содержимым. Приватность непубличного держалась ровно на одном — что работы нет в каталоге, — но слаг собирается из названия (`slugify_kz`), то есть подбирается, а не только утекает ссылкой. Побочные эффекты того же корня: черновику копились `views`/`recent_views` (один заход по прямому адресу — и работа приносит в каталог статистику, ещё не побывав у читателя), он ложился на полку библиотеки и получал закладку чтения, а действия по слагу — комментарий, полка, реакция, голос в опросе — отвечали на чужой черновик так же, как на витрину, потому что принимают слаг, а не объект. Отдельно: закрыть страницу «всем, кроме автора» было нельзя — модератор читает текст работы именно через неё, в админке у главы лежат номер, заголовок и число знаков, но не тело | **Видимость проверяется в одной двери — `story_by_slug(slug, viewer)`, и по умолчанию (`viewer=None`) она закрыта до публичного**: вызов, забывший спросить, кто смотрит, получает минимум, а не всё (fail closed). Непубличную работу видят двое — её автор (в любом статусе) и модератор (`is_staff`); всем прочим отдаётся **404, тот же ответ, что у несуществующего слага**: 403 подтверждал бы, что работа есть. Ту же дверь спрашивают все действия по слагу, включая реакцию и голос, которые резолвят главу, а не работу. Следы чтения непубличное больше не оставляет: `_count_view` выходит до записи, полка и закладка — тоже. Автору и модератору страница помечена плашкой `partials/story/preview_notice.html` (разные слова: автору «оқырман бұл бетті көрмейді», модератору «бет модерацияға ашылған»), иначе «Сайтта қарау» из кабинета читается как «уже опубликовано». Побочно вскрылось, что `story_by_slug` использовался в суите как универсальный резолвер по слагу, в том числе для черновиков: восемь таких мест переведены на авторскую дверь `story_by_slug_for_author` — там, где спрашивают своё, а не читательское. Первый шаг разбора авторского флоу ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md), находка C3); правило записано как BR-76 | [spec.md](spec.md) |
| **DEC-67** | Все три формы кабинета отвечали на ошибку одинаково: `messages.error` на каждую причину плюс `redirect` на ту же страницу. У редиректа нет тела, поэтому введённое пропадало целиком. В редакторе главы это означало буквально следующее: автор набирает текст, ошибается в заголовке — и получает **пустую форму** с тостом «Атауын және мәтінін жаз», то есть требованием написать ровно то, что только что стёрли. Воспроизведено в браузере: 3 840 набранных знаков, счётчик после отправки — «0 таңба». На баптаулар та же механика стоила дороже: одна отвергнутая обложка (а `accept="image/*"` пропускает SVG, который отвергает сервер) уносила аннотацию, возрастную отметку и теги. Рядом жила вторая половина проблемы — опрос главы: варианты приходили `request.POST.getlist('poll_option')` **мимо формы**, поэтому сказать о них было нечем, и вопрос с одним вариантом молча не сохранялся под тост «Жоба сақталды» — про главу правду, про опрос ложь. Плюс глава и её опрос писались двумя операциями без транзакции | **Ошибка возвращает форму, PRG остаётся только для успеха** (BR-77). Значения на странице читаются из формы, а не из объекта: иначе отказ показывал бы сохранённое и молча откатывал правку на экране (закрыто тестом `test_the_form_shows_what_was_typed_not_what_is_stored`). Ошибка стоит у своего поля — `state="error"` и `message` у `input_field`/`textarea` **уже существовали**, ими просто никто не пользовался; добавлен только `aria-invalid`. Тосты остаются за исходами операций, а не за валидацией. Опрос переехал в форму целиком: `PollOptionsWidget.value_from_datadict` читает `getlist` — механизм, ради которого повторяющийся `name` и существует, — а `PollOptionsField` и `ChapterForm.clean` проверяют границы (BR-POLL-02) и **говорят** о них. Один вариант теперь ошибка, а не тишина; вопрос без вариантов и варианты без вопроса — тоже. Глава и опрос сохраняются одной дверью `save_chapter` под `transaction.atomic`: для автора это одно действие, и состояния «глава есть, опроса нет» быть не должно. Отвергнутая форма не пишет ничего, включая побочное: набранный тег возвращается чипом через `preview_story_tags` (без записи), потому что заведённый `pending` пережил бы работу, которая не сохранилась; общее правило имён вынесено в `acceptable_tag_names`, чтобы чипы не расходились с тем, что сохранится. Попутно исправлены две находки прямо в переписываемых строках: в баптаулар дополнительный жанр сравнивался объектом со слагом (`g.slug == story.secondary_genre`) и потому **никогда** не подсвечивался, а в редакторе зелёное «Жоба сақталды» показывалось рядом с красным полем на странице, где ничего не сохранилось. Второй шаг разбора ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): C4, S7, S9, M9, V3) | [spec.md](spec.md), [ui.md](ui.md) |
| **DEC-68** | DEC-67 научил формы возвращать введённое, но сохранить текст по-прежнему мог только сам автор, нажав кнопку. Индикатор «Сақталмаған өзгеріс бар» честно показывал, что изменения не сохранены, — и ничего с этим не делал; в комментарии шаблона это было прямо записано как «реального запроса до Ф14 нет». Для подростка, пишущего с телефона, закрытая вкладка или разряженная батарея стоили всей главы. Прямолинейное решение — «писать в `Chapter` каждые несколько секунд» — **расширяло бы дыру модерации**: одобрение выдаётся работе один раз (C1/C2), поэтому у публичного сериала автосохранение выкладывало бы читателю недописанную главу само, без единого нажатия, а у новой главы — ещё и создавало бы её | **Автосохранение с debounce 3 с, доступное только непубличной работе** (BR-78); у публичной сервер не пишет вовсе, и редактор остаётся на страховке в браузере. Отдельная дверь `autosave_chapter` рядом с `save_chapter`: у автосохранения другие обязанности — оно не трогает опрос (затирать его содержимым полей, которых на экране может не быть, нельзя) и не требует законченности, поэтому проверяет его отдельная `ChapterAutosaveForm`, где обязательных полей нет. Ответ — JSON с номером главы: первый автосейв новой главы присваивает номер, и редактор обязан переключиться на него, иначе следующий заход завёл бы вторую главу (`history.replaceState` меняет и адрес страницы). Вторая страховка — `localStorage` на каждое нажатие: она работает там, где первая бессильна (офлайн, упавший сервер, публичная работа), и обёрнута в try/catch — приватное окно бросает на самом обращении, и уронить редактор ради страховки значило бы устроить ровно ту потерю, от которой она защищает. Найденная при загрузке версия **не подставляется молча**: показывается плашка с выбором «Қалпына келтіру / Керек емес», потому что незаметная подмена текста хуже заметной потери. Индикатор перестал быть декорацией — у него четыре настоящих состояния, и «сақталды» он говорит по ответу сервера со временем, а не по таймеру. Ловушка, пойманная только живым проходом (тесты Django JS не исполняют): внутри `@click` alpine-выражение вычисляется на **кнопке**, поэтому `this.$el.querySelector(...)` уходил внутрь неё и возвращал `null` — восстановление падало в консоль молча; корень компонента даёт `$root`. Третий шаг разбора ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): остаток C4) | [spec.md](spec.md), [ui.md](ui.md) |
| **DEC-69** | **Модерация выдавалась работе один раз и дальше не значила ничего.** Единицей публикации было произведение, а читают главы, и `Chapter` была одной строкой сразу для двух ролей — то, что пишет автор, и то, что видит читатель. Отсюда два подтверждённых прогоном обхода: вторая глава публичного сериала появлялась у читателя **в момент сохранения** (C1) — то есть весь длинный контент не проверял никто, — а переписанный одобренный текст доезжал тем же движением, вместе с возрастной отметкой и названием (C2). Правка во время очереди меняла то, что модератор в этот момент читал (V10), истории версий не было вовсе, и две вкладки перетирали друг друга молча (S10). Побочно это же связывало руки автосохранению: в DEC-68 его пришлось запретить публичным работам, потому что записанная глава немедленно уходила читателю | **Заведена `ChapterRevision` — снимок текста и его судьба (`draft`/`pending`/`approved`/`rejected`), а у главы появился `published_revision`: рабочая копия и опубликованное разошлись** (BR-79). Читателю отдаются только главы с одобренной ревизией и текст **из неё**; автору и модератору — рабочая копия, это их предпросмотр (BR-76). Отправка подаёт ревизии только тех глав, что разошлись с опубликованным, повторная — заменяет поданное; решение принимается по ревизиям, и отказ **не трогает уже стоящее у читателя**. Из этого следует главное изменение модели: **`Story.status` перестал назначаться и стал пересчитываться** по главам (`refresh_status`). Прежний `status_after_moderation` возвращал `NotPublished` любому отказу — и публичный сериал, чью новую главу вернули на доработку, исчезал бы из каталога целиком вместе с уже одобренными главами. Колонка при этом осталась: по ней идут `WHERE`, `ORDER BY` и четыре индекса каталога — тот же законный случай, что `Story.likes`; убрать её значило бы переписать каталог без выигрыша. «Аяқталды» переехало в `completed_by_author`: это слово автора, и хранить его в пересчитываемой колонке стало негде. Разошлись и числа: `Story.chapters` с объёмом чтения считают опубликованное (иначе карточка обещает «8 бөлім» при тексте на пять), `chapters_written` — написанное, и кабинет показывает автору его, иначе написавший три бөлім видел бы ноль (DEC-51). Ограничение автосохранения из DEC-68 снято: оно пишет рабочую копию, читателю невидимую. Миграция 0005 и `seed_demo` переносят существующее по одному правилу — что было видно, остаётся видным: публичной работе `approved`, работе на модерации `pending`, черновику ничего. Побочно всплыло, что «работа на модерации без единой главы» стало невозможным состоянием: демо-фикстура `aidana-erteg` его изображала и получила текст. Четвёртый шаг разбора ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): C1, C2, S10, V10) | [spec.md](spec.md), [architecture.md](architecture.md) |
| **DEC-70** | Возврат «на доработку» **не оставлял следа на рабочем экране**: работа падала в черновики, чек-лист снова горел зелёным, кнопка отправки была активна, и единственный экземпляр причины лежал в ленте уведомлений — автор должен был помнить замечание наизусть, пока правит (S1). Отличить возвращённое от нетронутого черновика в списке из восьми работ было нечем. Отозвать заявку было нельзя вовсе: заметив опечатку через минуту после отправки, автор мог только ждать модератора, чтобы тот вернул работу (S2). А сообщение об отказе перечисляло причины **на память** — «аннотация мен жас белгісі» — и врало всякий раз, когда не хватало текста; подписи пунктов существовали единственным экземпляром внутри `publish_panel.html`, и назвать недостающее могла только сама панель (S3) | **`NeedsWork` заведён шестым статусом** (BR-80) — DEC отменяет прежнее «шестого без нового DEC не будет»: пока модерация выдавалась работе один раз, «доработай» было исходом события и ничем в самой работе, а с пересчитываемым статусом (BR-79) это её настоящее состояние. Выводится из последнего решения, а не из новой колонки: колонка разошлась бы с лентой, где то же решение уже записано. У публичной работы `NeedsWork` не бывает — вернуть её в непубличное значит наказать читателя за то, чего он не видел. Замечание висит на странице управления до повторной отправки и уходит само, когда автор подал заново: оно про прошлую версию. Заявку теперь можно **отозвать** — ревизия возвращается в `draft`, то есть остаётся снимком текста и историей, а не исчезает; опубликованного отзыв не касается. Подписи чек-листа переехали в `domain.story.checklist_label` (BR-81) и оттуда приезжают и в панель, и в сообщение: у «текста» подпись зависит от формата — звать одночастную работу писать «алғашқы бөлім» значит предлагать несуществующее. Простыня `{% if item.key == ... %}` из шаблона ушла. Поймано при этом: `apply_moderation` пересчитывал статус **до** записи уведомления, то есть до того, как решение попадало в базу, — и возвращённая работа оставалась неотличимой от черновика до следующей случайной правки. Пятый шаг разбора ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): S1, S2, S3, V11) | [spec.md](spec.md), [ui.md](ui.md) |
| **DEC-71** | DEC-23 объявил админку единственным инструментом модерации, и как **дверь решения** она работала: причина обязательна, ручная правка статуса предупреждает, пропущенные работы названы числом. Но **работу модератора она не поддерживала**. Текста, по которому принимается решение, среди её страниц не было вовсе: `ChapterInline` показывает номер, заголовок и число знаков, а тело главы лежало страницей глубже — то есть читать присланное приходилось либо через публичный URL (что работало ровно потому, что была дыра C3), либо кликая по главам поодиночке. Повторная подача была неотличима от первой: что автор изменил после замечания — вопрос без ответа, и работу перечитывали целиком. Очередь не приоритизировалась: `list_filter` по статусу не отвечает «кого мы держим дольше всех», а с BR-79 фильтр по статусу вдобавок **прятал** дописанную главу публичного сериала — та остаётся `OnProcess`. Двое модераторов, открывшие одну работу, узнавали друг о друге по результату. И след решения был один — `Notification`, адресованное автору: у него нет того, кто решение принял, и вопрос «кто это одобрил» ответа не имел | **Заведён раздел `/moderation/`** (BR-82) — отменяет DEC-23 в части «кастомного UI до V2 не будет». Открыт `is_staff`, отказ — 404, а не 403: существование раздела не подтверждается тому, кому он закрыт (то же правило, что у чужого черновика, BR-76). Очередь собрана из **поданных ревизий**, порядок один и не настраивается — дольше всех ждущий первым; оси (`waiting` / `repeat` / `first` / `mine`) отвечают не на «как отсортировать», а на «что я сейчас беру». На карточке решения рядом с тремя кнопками лежит всё, по чему решают: текст поданной ревизии, **сравнение с опубликованной по абзацам** (`domain/moderation.paragraph_diff` — абзац, а не символ и не строка: посимвольный diff по казахской прозе нечитаем, построчный распадается на обрывки) и прошлые решения по работе. Заведены две модели: `ModerationDecision` — акт с автором, датой и охватом («одобрено 3 бөлім»), как `AwardGrant` (DEC-46), и `ModerationClaim` — «взял в работу», которое **предупреждает, но не запрещает**: забытая метка не должна блокировать очередь до вмешательства администратора, чужую метку не перебивают и не снимают, своё решение снимает её само. Действия админки остались запасным путём и ведут в ту же дверь `apply_moderation`, но её очередь теперь тоже собирается по ревизиям, а не по статусу. Готовые причины возврата — защита от главной беды обязательного поля: когда причину требуют, а писать долго, в неё попадает «жоқ». Побочно поправлено `kk_ago`: у метки, которую ставят и снимают за минуты, «бүгін» не сообщало ничего — добавлено нижнее деление «жаңа ғана», и появляется оно только когда минуты посчитаны (`kk_ago(0)` без них остаётся «бүгін»). Шестой шаг разбора | [spec.md](spec.md), [architecture.md](architecture.md) |
| **DEC-72** | Номер главы нёс три роли сразу: адрес в кабинете (`/write/<slug>/chapter/<number>/edit/`), порядковый номер для читателя и уникальный ключ в базе — и адрес не проверял, что глава с этим номером вообще существует. GET на несуществующий номер рисовал пустой «новый» редактор, а POST по тому же адресу заводил главу с этим самым номером — дыру в нумерации (S5). Удалить или переставить главу было нельзя вовсе: таких маршрутов не было (M3). А `single`-формат ограничивал только обратный переход — прямой `/chapter/new/` был открыт всегда и заводил второй текст в обход интерфейса (S6) | **Кабинет адресует главу стабильным `pk`** (BR-83) — несуществующий или чужой id теперь 404, а не пустой «новый» редактор, который на POST создавал главу с произвольным номером. Читательские адреса (`story_detail?chapter=`, реакция, опрос, `chapter_number` комментария) номером и остались — там он не участвовал в дыре. Заведено поле `Chapter.position` — порядок автора, отдельно от отображаемого номера; **главу можно удалить и переставить** (BR-84), после чего номер пересчитывается контигом 1..N по позиции (`queries/write._renumber_chapters`), а статус работы — следом (удаление последней главы возвращает её в черновик). `single` держит не больше одной главы и на уровне данных, а не только формы настроек (BR-85): прямой запрос второй главы редиректит на существующую или дописывает её же, не заводя дубль. Седьмой шаг разбора ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): S5, S6, M3) | [spec.md](spec.md) |
| **DEC-73** | `RASTER_ONLY` (`FileExtensionValidator`) проверял только расширение файла — содержимое под именем `.png` могло быть чем угодно. Лимита размера не было (`FILE_UPLOAD_MAX_MEMORY_SIZE`/`DATA_UPLOAD_MAX_MEMORY_SIZE` не заданы), пережатия — тоже. Замена обложки не перезаписывала файл: Django добавлял суффикс к имени, старый оставался в `media/` навсегда; удаление работы файл тоже не убирало (S8). Форма настроек начинала себя с обложки — единственного необязательного поля, пока обязательные (аннотация, жас белгісі) стояли ниже (V13). Убрать обложку, не заменив её, было нечем — пустая загрузка значила только «не меняем» | **Заведён Pillow** (решение принято осознанно — раньше от него отказывались «ради одного поля», но требование реальной проверки контента и пережатия без него не выполнить) — и правило заведено сразу на все четыре растровых поля платформы: `Story.cover`, `User.avatar`, `Contest.poster`, `ContestAward.image` — у них был один и тот же пробел. `validate_raster_image` (`core/uploads.py`) декодирует файл по-настоящему (`Image.load()`, не `.verify()` — только полная распаковка ловит и усечённый файл, и Pillow-бомбу) и проверяет размер (BR-86). Пережатие — в формат **по расширению**, не по факту найденного в байтах: иначе `.png`-адрес мог бы нести JPEG внутри. Уборка старого файла — **сигналами** (`core/media_cleanup.py`, по образцу `core/counters.py`), а не вызовом в `queries/write.py`/`queries/profile.py`: `poster`/`image` грузятся только из `admin.py`, и правка самой админки для этого не потребовалась ни строкой — тот же довод, по которому счётчики держатся сигналами, а не ручным вызовом в доменных функциях. Заведено явное `remove_cover`/`remove_avatar` — третье состояние рядом с «новый файл» и «пусто значит не меняем». Обложка в форме настроек переехала вниз, после последнего обязательного поля. Заодно выправлен сам BR-46: числа «от 512×512, до 1 МБ» ни разу не проверялись кодом — заменены ссылкой на общий лимит BR-86. Восьмой шаг разбора ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): S8, V13) | [spec.md](spec.md) |
| **DEC-74** | Живой счётчик редактора считал JS-строку с `\n`, сервер — пришедшее по HTTP `\r\n`: объём расходился на число абзацев и системно завышал `char_count`, а с ним `read_minutes`, фильтр каталога и условия конкурса (V1). `Chapter.save()` не трогал `Story.updated_at` — автор, писавший часами, не поднимался в кабинете в списке «что трогал последним», хотя DEC-40 уже вводил сортировку по этому полю (S4). Слаг фиксировался при создании навсегда — переименованная до публикации работа жила по адресу первого черновика (M1). Создание черновика ничем не ограничивалось — 25 POST подряд заводили 25 работ (M2). Кабинет отвечал одной и той же карточкой «табылмады» кодом 200 и гостю, и уже вошедшему на чужой или несуществующий слаг: гостю она не говорила, что делать, а POST мимо формы на чужой слаг тихо проваливался в неё же вместо 403/404 (M4–M8). И `delete_confirm_modal` до открытия несла пустой `action` — не «никуда», а «текущая страница» по HTML-спеке; сегодня безопасно только потому, что кнопка скрыта `display:none` (V12) | **Нормализация — в `Chapter.save()`**, одно место: `\r\n`/`\r` схлопываются в `\n` до подсчёта `char_count`. **`Story.updated_at` трогает `queries/write._touch_story`** — в `save_chapter`/`autosave_chapter`, а не в `Chapter.save()`: там же работает демо-сид, который намеренно бэкдейтит `updated_at` отдельным `update()` сразу после заполнения корпуса (см. docstring `_seed_stories`), и тронуть поле на каждую главу означало бы молча стирать выставленную давность. **Слаг регенерируется при переименовании, пока работа непубличная** (BR-87) — `_unique_story_slug` с исключением себя из проверки уникальности; после первой опубликованной главы название и адрес расходятся насовсем. **Заведён потолок `MAX_DRAFT_STORIES` = 15** (BR-88) на `NotPublished`-работы автора — проверяет `NewStoryForm.clean()` (форма проверяет, `core.data` пишет, как и везде), а не слой данных. **Кабинет разведён на `auth_gate` и 404** (BR-89, тот же приём, что у `my_stories`/`new_story`, `library`, `notifications`): гостю — приглашение войти кодом 200, вошедшему на чужой или несуществующий слаг — 404, как у непубличной чужой работы (BR-76); общая заглушка с кодом 200 на оба случая ушла из трёх шаблонов. У модалки удаления — страховка `@submit`, отменяющая отправку при пустом `confirm_url`: защита на случай, если скрытие кнопки однажды сменят с `display:none` на `opacity`/`visibility`. Девятый шаг разбора ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): S4, M1, M2, M4–M8, V1, V12) | [spec.md](spec.md) |
| **DEC-75** | Тост при отказе SVG называл читателю внутренний код правила — «SVG қабылданбайды **(BR-46)**» (V2). Уведомление всплывало справа снизу ровно поверх «Сақтау»/«Болдырмау»: панели действий по проекту прижаты вправо, и тост вставал на то же место (V4). Пункт чек-листа «Аннотация жаз» вёл на верх `/settings/` целиком — адрес был, поля в нём не было, автор искал его сам (V6). Подпись поля тегов мешала языки: «Тегтер — ключевые сөздер» (V7). Шапка непубличной работы показывала «0 оқылым · 0 реакция · 0 пікір» — три нуля читались как провал на пустом месте, хотя других значений там быть не может (V8). «Модерацияға жіберу» в редакторе первой главы новой работы стояла активной, хотя аннотации и жас белгісі ещё нет и быть не может, — гарантированный отказ сразу после нажатия (V14). Согласие с правилами публикации — юридически значимая строка — стояло 12px серым по правому краю **под** кнопкой, самым незаметным элементом экрана (V15) | Код правила убран из сообщения `validate_raster_image` — ссылка на BR-46 остаётся в докстринге и в docs/, не в тосте. **`toast_host` переставлен налево на десктопе** (`sm:left-6` вместо `sm:right-6`) — один общий компонент, а не правка каждой формы по отдельности, поскольку прижатые вправо панели действий это общий приём проекта, не особенность одной страницы. **Пункты чек-листа, ведущие в баптаулар, несут якорь своего поля** (`checklist_links` — `#t-annotation`, `#audience`, `#cover`, `#tags`) — браузер сам докручивает до цели; **добавлен фокус** обычным `DOMContentLoaded` в `components.js` (не `alpine:init` — цель обычный серверный DOM, Alpine над ним ничего не пересоздаёт), общий на любой будущий якорь, не только чек-лист. Подпись тегов переведена целиком на казахский. **Шапка `manage_story` разведена по статусу** тем же приёмом, что уже был в `my_story_row.html` кабинета: «тексеруде» / «жарияланбаған» вместо трёх нулей, настоящие метрики — только у публичного. **Кнопка отправки в редакторе гейтится `can_submit_for_review`/`missing_for_review`** — тот же вопрос и тот же вид ответа (неактивная кнопка с подсказкой), что уже даёт `publish_panel.html` на manage_story; когда чек-лист закрыт, а слать нечего (уже всё опубликовано), кнопка молчит совсем, а не остаётся мимо-кнопкой. Бюджет `chapter_editor` поднят с 6 до 12 запросов — тот же двойной счёт чек-листа, что уже оплачен на manage_story (`can_submit_for_review` зовёт `missing_for_review` повторно внутри себя). **Согласие переехало перед кнопкой**, размером обычного текста (`text-sm text-slate-700`), а не подвалом после неё. Десятый шаг разбора ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): V2, V4, V6, V7, V8, V14, V15) — новых BR не заводит, всё уже требовали BR-46/76/79/81 и «интерфейс — казахский» из ui.md, здесь только приведение кода в соответствие | [ui.md](ui.md) |
| **DEC-76** | На 375×812 липкая панель редактора главы (три кнопки в столбик плюс индикатор) и плавающая пилюля `mobile_nav` вместе занимали около половины экрана. От textarea `rows=20` со своим внутренним скроллом поверх скролла страницы оставалась полоса в четыре строки, и полупрозрачный `bg-white/95` с `backdrop-blur` просвечивал набранным текстом — полоса читалась грязно (V5). Подросток, пишущий с телефона, — заявленный основной автор платформы; NFR-40…42 уже требует адаптивность от 375px, и здесь был её пробел | **Панель — компактная на мобильном**: подписи кнопок и индикатора уходят в `sr-only` (не `hidden` — это визуальное сжатие, не вырезание из accessibility-дерева и не молчание `aria-live`), остаются иконка и цвет; с `sm` подписи возвращаются `not-sr-only`. Фон панели — сплошной `bg-white` на мобильном, `bg-white/95` + `backdrop-blur` только с `sm`, где панель уже не в пол-экрана. **Коробка текста — высотой во вьюпорт** (`min-h-[62vh]`, `sm:min-h-[26rem]`) вместо `rows=20`: жёсткий счёт строк подменён на минимальную высоту, а `[field-sizing:content]` — прогрессивное улучшение поверх нее: на поддерживающих браузерах textarea растёт вслед за текстом по-настоящему и внутренний скролл не наступает вовсе, на остальных остаётся просто высокая коробка. Одиннадцатый шаг разбора ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): V5) | [ui.md](ui.md) |
| **DEC-77** | Раздел 5.7 (более ранний, амбициознее набросок плана) рисовал целевой экран тремя равноправными колонками — список глав, редактор, чек-лист готовности. Но каркас портала (`docs/ui.md`) уже фиксирует: колонка контента `max-w-[860px]`, свой больший `max-w` в шаблоне страницы запрещён, у WRITE-страниц рейла нет вовсе (DEC-48). Буквальные три колонки в 860px не помещаются — редактору прозы нужно от 400px на комфортный набор, а 860 минус два узких списка такого не оставляет | **Список глав — узкая колонка (240px) слева от редактора; чек-лист третьей колонкой не становится** — решение принято с пользователем, не молча: конфликт с уже задокументированным правилом каркаса не повод его тихо обойти. Чек-лист остаётся, где был, — во всю ширину над блоком редактора и списка, — но `publish_panel.html` стал `<details>`, свёрнутой по умолчанию, когда панели нечего показывать активного (нет незакрытых обязательных пунктов, нет замечания модератора, и сам блок отправки не рисуется). Условие вынесено в Python (`_checklist_actionable`, `core/views/write.py`), не в шаблон: Django `{% with %}` не считает булевы выражения (`or`/`==`) — было бы синтаксической ошибкой. Строка списка глав переделана — пять иконок в ряд (реордер ×2, редактировать, посмотреть на сайте, удалить) не помещались в 240px; теперь вся карточка — ссылка с тем же `hx-get`/`hx-target="#editor-pane"` переключением, что раньше нёс только карандаш (отдельная иконка «Өңдеу» стала лишней), «Сайтта қарау» из строки убрана (путь к читателю остаётся через шапку), реордер и удаление (BR-84) остались компактным рядом под текстом. Двенадцатый шаг разбора, в процессе — этап 11 целиком ещё не закрыт ([AUDIT-WRITE-FLOW.md](../AUDIT-WRITE-FLOW.md): 11.1, 11.2) | [ui.md](ui.md) |

Этот файл — реестр **закрытых** вопросов (решения `DEC-NN` выше). Отдельного реестра открытых вопросов сейчас нет.
