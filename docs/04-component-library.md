# 04 · Библиотека компонентов

> `Обновлён: 2026-08-22` · `Сверен с кодом: b43b190`

Спецификация переиспользуемых UI-компонентов: назначение, входные параметры (props), состояния и поведение. Компоненты обязаны использовать токены из модуля 02.

## 4.1 Каталог компонентов

**Параметры каждого компонента документированы в нём самом** — в блоке `{% comment %}` в первых строках файла. Здесь их дублировать нельзя: копия расходится с оригиналом молча, и именно так этот модуль три месяца описывал `PrimaryButton`, `InputValidated` и `DeskBookCardWide`, которых в коде нет.

Все 59 файлов в `templates/components/`. Обязаны использовать токены модуля 02; прямые `rgb()` запрещены (DEC-05).

### Бренд и типовые элементы

| Файл | Назначение |
|------|-----------|
| `logo.html` | Логотип 36×36 + «Balaproza», опц. подзаголовок. Ведёт на главную |
| `button.html` | Единственная кнопка. `kind`: primary (заливка brand) / secondary (рамка) |
| `icon.html` | Обёртка над спрайтом, см. 4.2 |
| `badge.html` | Базовая пилюля-бейдж, основа для `status_badge` |
| `section_header.html` | Заголовок секции + eyebrow + ссылка «барлығы →» с тап-зоной 44px |
| `pagination.html` | ‹ 1 2 3 … › , использует фильтр `page_range` |

### Произведения

| Файл | Назначение |
|------|-----------|
| `cover_placeholder.html` | **Двухрежимная обложка.** Если `story.cover` задан — `<img src="/media/…">`; иначе типографическая плашка OKLCH по `primary_genre.hue` + буква + «корешок». Обязателен везде, где показывается обложка: `{% static story.cover %}` напрямую запрещён |
| `book_card_small.html` | Вертикальная карточка для рядов и каруселей. Чип длительности на обложке |
| `book_card_wide.html` | Горизонтальная карточка для списков каталога. **Mobile-first**: до `sm` обложка 88×132, мета сжата в одну строку через «·», аннотация в две строки, теги скрыты стилем, счётчики через `compact_count`; с `sm` — полная раскладка 120×180. Брейкпоинтов не было вовсе: на 375px колонке контента оставался 171px, семь блоков подряд разворачивались в ~430px высоты, и в экран влезала одна карточка |
| `library_row.html` | Строка библиотеки с прогрессом чтения |
| `my_story_row.html` | Строка авторского кабинета: обложка слева на всех ширинах, статус, когда трогали, метрики за две недели, два действия и меню. У непубличной работы вместо нулевых метрик — состояние процесса (DEC-40) |
| `my_story_menu.html` | Меню строки кабинета: «Сайтта қарау» (только для `story.is_public`) · «Баптаулар» · «Жою». Три точки помечают контейнер, а не действие — §4.2 |
| `attention_item.html` | Один сигнал полосы «Назарыңды күтеді» (FR-WRITE-08). Ссылка только когда за сигналом одна работа |
| `stat_pill.html` | Иконка + значение (лайки / просмотры / комментарии), `--color-slate-600`. `label` — полная озвучка с числом («4 821 ұнату»), обязателен везде, где пилюля несёт данные: иконка декоративная, значение под `aria-hidden`, и без подписи метрика пропадает из озвучки целиком. Подпись живёт в `sr-only`, а не в `aria-label`: у `<span>` с role=generic имя не выставляется |
| `stat_strip.html` | Компактная полоса из четырёх чисел для мобильного потока. Нужна там, где показатели живут в рейле: рейл появляется с `xl`, и до этой ширины цифры пропадали |
| `status_badge.html` | Справочник статусов, см. 4.3 |
| `genre_chip.html` | Цветной чип жанра (OKLCH, модуль 03). DEC-14: единственный компонент жанра |
| `reaction_bar.html` | Пять реакций на главу (FR-STORY-12, DEC-32), только при открытой главе. Отменяет прежний `chapter_like.html`. Каждая реакция обязана нести подпись словом: эмодзи запрещены, монохромная иконка 20px без подписи неразличима |
| `chapter_poll.html` | Необязательный опрос автора под главой (FR-STORY-13, DEC-33). Три ветки по состоянию: closed (результаты всем + ссылка на главу с ответом) / гость (варианты ведут на login) / авторизованный (бюллетень, проценты после голоса) |

### Теги (модуль 11)

| Файл | Назначение |
|------|-----------|
| `tag_chip.html` | Нейтральный slate-чип с `#` — намеренно отличается от цветного `genre_chip`. Pending получает пунктирную рамку и бейдж «проверкада» |
| `tag_list.html` | Ряд чипов произведения. **Скрывает pending от не-автора** — здесь живёт BR-TAG-07 |
| `tag_input.html` | Alpine: автокомплит по `accepted`, чипы с ×, счётчик `N/10`, inline-валидация длины, блок-листа и дублей |

### Формы

| Файл | Назначение |
|------|-----------|
| `input_field.html` | Поле ввода. `state`: default / success / error — три состояния валидации обязательны (DEC-13) |
| `textarea.html` | Многострочное поле. Счётчик символов обязателен там, где есть лимит (BR-16, BR-17) |
| `checkbox.html`, `radio_option.html`, `toggle.html` | Управляющие элементы; нативный input под `sr-only`, стилизуется обёртка через `peer` |

### Социальное

| Файл | Назначение |
|------|-----------|
| `comment.html` | Комментарий: аватар, имя-ссылка, текст, живой лайк (BR-31), меню трёх точек и форма ответа. Набор пунктов меню зависит от владельца — свой/чужой/гость (BR-33). Один уровень ответов (BR-30) держится самим компонентом через `depth`, бейдж «Автор» (BR-32) |
| `comment_login_gate.html` | Заглушка вместо поля ввода для гостя |
| `avatar.html` | Буквенные инициалы на OKLCH-фоне. Hue детерминирован длиной seed (`username + name`) — один автор всегда одного цвета |
| `author_row.html` | Строка автора в списке: аватар, имя, `@username`, число работ. `<li>` — включается внутрь `<ul>`. Общая для рейла «Жазылулар» и страниц подписчиков/подписок (FR-PROF-10); `compact` — плотный вариант для рейла. Карточку «Жаңа авторлар» на главной сюда не сводим: там вертикальная карточка в скроллере с био и двумя счётчиками — другой объект, а не другой размер этого |
| `notification_item.html` | Строка уведомления, группировка по времени (BR-70) |
| `award.html` | Награда автора (FR-PROF-06, DEC-43, DEC-46) — обвязка: кнопка с тултипом либо немая плитка. Тултип открывается по наведению, фокусу **и тапу** (BR-ACH-06). Два класса знаков: `art` — системный, рисунок из спрайта на постаменте; `image` — награда конкурса, медальон с загруженной эмблемой. Спрайт нужен только режиму `art` |
| `award_art.html` | Сама картинка награды без обвязки. Вынесена, потому что в `award.html` два режима подачи, а рисунок в них один: две копии разошлись бы. Медальон без файла показывает типографическую заглушку первой буквой номинации |
| `tooltip.html` | Всплывающая подпись. Своего `x-data` не заводит — живёт внутри предка с булевым `open`, и предок обязан быть `relative` |

### Конкурсы

| Файл | Назначение |
|------|-----------|
| `contest_card.html` | Карточка конкурса: обложка, бейдж фазы, отсчёт, приз, число заявок; у завершённого — строка победителей (FR-CONT-01, 08) |
| Номинации конкурса | Не отдельный компонент: `stub_data.ContestAward` рендерится через `award.html` в режиме `image` (BR-44, BR-46, DEC-46) |
| `contest_status.html` | Бейдж одной из **четырёх** фаз плюс отсчёт (BR-40, DEC-45). Принимает `contest`, а не разобранные `status`/`days_left`: фаза и отсчёт выводятся из дат самим объектом, и передавать их по отдельности значит дать шаблону шанс склеить несуществующую пару |
| `countdown.html` | Отсчёт дней. `suffix` по умолчанию «күн қалды»; у ещё не открывшегося конкурса счёт идёт до старта, и подпись там своя — иначе «12 күн қалды» означало бы, что приём вот-вот закроется |

### Оверлеи и системное

| Файл | Назначение |
|------|-----------|
| `login_modal.html` | Вход через Telegram |
| `report_modal.html` | Жалоба на контент, событие `open-report` (BR-33) |
| `delete_confirm_modal.html` | Подтверждение удаления произведения, событие `open-delete-confirm`. Копия сшита с удалением (красная кнопка, корзина) — под другое действие не переиспользуется |
| `withdraw_confirm_modal.html` | Подтверждение отзыва заявки с конкурса (BR-23b), событие `open-withdraw-confirm`. Отдельный компонент, а не параметр предыдущего: отзыв ничего не удаляет, и красная корзина пугала бы там, где пугать нечем |
| `search_popup.html` | Quick-search по Cmd+K или событию `open-search`. Индекс — lazy-fetch с `/api/search-index.json` при первом открытии, чтобы не утяжелять каждый HTML |
| `toast_host.html` | Глобальный приёмник: `$dispatch('toast', {kind, text})`. Подключён раз в `base.html` |
| `share_button.html` | `navigator.share()` на mobile, dropdown Telegram / WhatsApp / copy на десктопе |
| `segmented_control.html` | Реальное переключение через `?tab=`, не псевдо-табы (DEC-15). Разметка — `<nav>` + список ссылок + `aria-current="page"`; ролей `tablist`/`tab` тут нет и быть не должно: они обещают скринридеру `role="tabpanel"` и переключение стрелками без перезагрузки, а это навигация по URL. Закрыто `test_template_lint.TabRolesPromiseAPanel`. У элемента может быть готовый `href` — он перебивает пару `base_url`/`param`; нужен там, где разделы различаются путём, а не состоянием одной страницы (FR-PROF-10) |
| `school_links.html` | Блок внешних ссылок, 3 layout: list / grid / inline (DEC-22) |

Все модалки: затемнённый бэкдроп, закрытие по `Esc` и клику вне, `aria-label="Жабу"` на крестике, focus-trap (NFR-05).

### Состояния (DEC-17)

Обязательны на всех data-зависимых страницах. Опт-ин для просмотра — `?state=loading|error`, showcase — `/_design/states/`.

| Файл | Назначение |
|------|-----------|
| `skeleton_box.html`, `skeleton_circle.html`, `skeleton_text.html` | Примитивы: прямоугольник, круг, строки текста |
| `skeleton_book_card_small.html`, `skeleton_book_card_wide.html` | Скелетоны карточек. **Сетка обязана совпадать с сеткой контента** — иначе страница дёргается при подстановке; закрыто тестом. Включая брейкпоинты: `skeleton_book_card_wide` повторяет и мобильную обложку 88×132, и двухстрочную аннотацию |
| `skeleton_comment.html`, `skeleton_chapter_text.html`, `skeleton_my_story_row.html`, `skeleton_notification_item.html` | Скелетоны под конкретные строки |
| `empty_state.html` | Иконка, заголовок, подсказка, опц. CTA. **Единственный пустой стейт** — `my_stories` держала свою копию с тем же заголовком, что у `profile_me`, но другим текстом |
| `error_state.html` | Ошибка загрузки + «Қайта көру». Есть `compact`-вариант для inline |
| `auth_gate.html` | Что видит гость на личной странице (LIB, NOTIF, PROF, WRITE, CONT). Параметры: `reason` («Кітапхананы көру үшін», без точки) и опц. `title`. Блок был скопирован в девяти шаблонах слово в слово. Цвет остался сырым `amber-*`: пара `status-warning-*` рассчитана на пилюлю 22px и на панели кричит — единственное место с внереестровым цветом, ждёт своей пары в `@theme` |

> ✅ **Решение DEC-14.** В прототипе сосуществовали `GenrePill` (плоский текст) и `GenreChip` (цветной). Остаётся только `GenreChip`.

> ✅ **Решение DEC-25.** Левый sidebar исключён. Навигация — [07](07-layout-navigation.md): шапка, footer как карта сайта, mobile bottom nav. Состав слотов описан там же в §7.6 и больше нигде.

## 4.1a Награды

Второй спрайт — `components/awards/_sprite.html`, десять `<symbol id="award-…">`. Подключается **не в `base.html`**, а в `partials/profile/_achievements.html`: иллюстрации нужны одному разделу, а иконочный спрайт уже висит инлайном ~22 КБ на каждой странице. Бюджет — ≤2,5 КБ на награду, ≤25 КБ на спрайт (фактически ~17 КБ).

Это **не иконки**. Иконка монохромна, живёт на `currentColor` и читается при 16px; награда цветная, с градиентами и бликом, и ниже 48px рассыпается. Смешивать их в одном спрайте нельзя — разойдётся и вес, и правила.

Система из трёх слоёв:

| Слой | Что даёт |
|------|----------|
| Постамент | Одинаковая геометрия у всех десяти. Именно он превращает набор в набор, а не в десять клипартов рядом |
| Эмблема | Уникальный силуэт: книга, перо, стопка томов, медаль, кубок, звезда, стела с числом |
| Металл | Ступень ценности — бронза / серебро / золото (BR-ACH-02, палитра [03 §3.6](03-genre-color-system.md)) |

Уникальных рисунков **семь, а не десять**: четыре ступени оқылым — один рисунок-стела, у которого меняются число на табличке и металл.

**Постамент инлайнится в каждый `<symbol>`.** Вложенный `<use>` на группу в `<defs>` рендерится не во всех движках: при проверке рендером постамент молча исчез, оставив эмблемы висеть в воздухе. Повторение разметки здесь дешевле, чем зависимость от поведения движка.

Слаг иллюстрации приходит из `stub_data.achievements_of` — то есть из данных, где опечатку заметить негде. Закрыто `test_template_lint.IconNamesExistInSprite`: `test_award_art_exists_in_sprite` ловит ссылку на несуществующий символ, `test_award_sprite_has_no_orphan_symbols` — символ, на который никто не ссылается (мёртвый вес на странице).

## 4.2 Иконки

Один SVG-спрайт, подключённый один раз в `base.html` сразу после `<body>`. Каждая иконка — `<symbol id="icon-…">`, вызывается через `components/icon.html with name="…" only`. Базовый стиль — `currentColor` (цвет берётся из CSS `text-*`), `viewBox=24`.

**`only` обязателен.** Без него `{% include %}` наследует родительский контекст, и `label` кнопки или бейджа доезжает до иконки: она становится `<svg role="img" aria-label="…">` с той же подписью, что и текст рядом, — скринридер читает её дважды. Осознанно `label` иконке не передаёт ни один вызов в проекте, так что все такие имена были утечкой. Закрыто `test_template_lint.IconIncludesAreIsolated` и `IconLabelsDoNotDuplicateText`.

Состав:

- **Навигация:** Search, ArrowRight/Left, AngleLeft/Right, ChevronLeft/Right, X
- **Действия:** Home, Bookmark/-filled, Bell, UserCircle, Plus, Pen, Cog, Trash, Upload, PaperPlane, List, Check, Adjustments, Book, ArrowRightToBracket
- **Метрики:** ThumbsUp/-filled, Eye, MessageCaption, Heart/-filled
- **Меню:** DotsHorizontal, DotsVertical
- **Модерация и безопасность:** Flag (жалоба, BR-33), Shield (плашка «защищено правилами модерации»)

> **Правило: три точки — только на триггере меню.** `DotsHorizontal`/`DotsVertical` означают «ещё варианты» — это иконка контейнера, а не действия. Ставить их на конкретный пункт нельзя: в меню из трёх точек получается «ещё варианты → ещё варианты». Именно так на «Шағым жіберу» два раза оказались точки вместо флажка. Флажок для жалобы — конвенция YouTube, Instagram, Reddit и GitHub; восклицательный знак не годится, он читается как «ошибка» и пересекается со `status-error`. Правило закрыто тестом `test_story.ReportUsesItsOwnIcon`.

> `DotsVertical` сейчас не вызывается ниоткуда — оставлен намеренно, под вертикальный триггер меню, если он понадобится.
- **«Настроения» для коллекций (DEC-25 ассеты):** Drop, Backpack, Planet, Cityscape, Feather, Fir, Skull, Smile. Каждой коллекции в `stub_data.Collection.icon` соответствует один из них (например, «Көзжасты түн» — `drop`, «Мектеп күнделігі» — `backpack`).
- **Реакции на главу (DEC-32):** Smile (Күлдім), Drop (Жыладым), Heart-filled (Жүрегім), Feather (Шабыт), Sparkle (Таңғалдым). Три из пяти переиспользуются из «настроений» — иконка может служить двум ролям, потому что рядом всегда стоит подпись словом.
- **Бренд:** Telegram (с градиентом), Instagram, TikTok, YouTube

> ✅ **Решение DEC-09b.** Набор смешивает залитые (`fill`) и контурные (`stroke`) иконки. Фиксируется единый стиль — **контурный (outline) 2px** для всех иконок интерфейса; залитые остаются только там, где это семантически оправдано (заполненный `Heart`/`Bookmark` = активное состояние «лайкнуто»/«сохранено»).

> ✅ **Правило (no-emoji).** Стандартные эмодзи (☀️ 📖 🇰🇿 🕯️ 😢 🎒 👽 🌆 ✍️ 🎄 🧟 😄 и т.п.) в шаблонах, stub_data и любом контенте проекта **запрещены** — они выглядят дёшево и роняют уровень дизайна. Если нужна «иконка настроения» (как в коллекциях) — добавляем SVG-symbol в спрайт. Если нужна разметка для пользовательского контента (комментарии) — оставляем как есть, пользователи могут писать эмодзи в своих текстах.

## 4.3 Унифицированный каталог (DEC-27)

После DEC-27 search/genre/tag/catalog рендерятся одним шаблоном `pages/catalog/catalog.html` через тонкие view-обёртки над `_render_catalog()`. Каркас собирается из переиспользуемых партиалов:

| Партиал | Назначение |
|---------|-----------|
| `partials/catalog/_hero_search.html` | Search-режим: поле ввода + echo запроса |
| `partials/catalog/_hero_genre.html` | Genre-режим: hue-tinted блок, имя жанра, счётчик |
| `partials/catalog/_hero_tag.html` | Tag-режим: slate-блок + `#`-префикс, usage_count |
| `partials/catalog/_hero_catalog.html` | Нейтральный hero для `/catalog/` |
| `partials/catalog/_book_list.html` | Список `book_card_wide` + empty-state (переиспользуется И коллекциями) |
| `partials/catalog/_filter_panel.html` | Пять осей чипами (цикл по `filter_groups`) + q (refining) + genre chip-cloud + popular tags chip-cloud. Параметр `variant`: `rail` сабмитит по `@change`, `sheet` копит черновик и применяет кнопкой (FR-CAT-07) |
| `partials/catalog/_filter_sheet.html` | Mobile bottom-sheet с тем же `_filter_panel` в `variant="sheet"`. Триггер: `$dispatch('open-catalog-filters')`. Кнопка «Нәтижелерді көрсету» стоит после жанров и тегов и связана с формой через `form=` |
| `partials/right_rail/catalog.html` | Wrapper рейла: оборачивает `_filter_panel` в card |

Комбинация фильтров — через query string: `/genres/triller/?tag=mektep&status=Published`. Тонкая обёртка — `_render_catalog(request, mode='...', genre_slug=..., tag_slug=...)`.

## 4.4 Статусы произведения

Собирать статусы вручную запрещено — только через `components/status_badge.html`. Статусная модель — BR-10/BR-11 в [08](08-business-rules.md), канонические тексты и обоснование формулировок — [16 §16.3](16-content-voice.md).
